# PROGRESS — Claudo II: Lord of Instruction

Журнал оркестратора. Одна строка на **принятый** тикет. Тикет попадает сюда
только после того, как оркестратор сам прогнал проверки из
`ORCHESTRATOR_PROMPT.md` §5 — отчёт сабагента сам по себе строкой не является.

**Состояние: M1 — World and navigation ЗАКРЫТ, 14 из 14 тикетов, гейт зелёный
целиком. M0 — Skeleton ЗАКРЫТ, 19 из 19.**

**Гейт M1, прогнан оркестратором 2026-07-29:**

| # | Пункт гейта | Результат |
|---|---|---|
| ① | `npm run build` | зелёный |
| ② | `npm test`, несколько прогонов подряд | **зелёный, 5 + 3 прогона подряд**, каждый 581 + 75 = 656 тестов, обе стадии `fail=0` |
| ③ | `check-imports` + `check-fixed`, с `src/world/` и `src/nav/` корнями | PASS / PASS, **корней 6** (было 3), 26 файлов |
| ④ | `12.P08` — `nav.rebuild ≤ 3 мс` | **p99 = 1.134 мс** (p50 0.847), 300 сэмплов |
| ⑤ | `12.P09` — A\* худший случай ≤ 1 мс | **p99 = 0.044 мс** (p50 0.020), 300 сэмплов, **и каждый сэмпл проверяет, что путь реально найден** |
| ⑥ | 200 прогонов по тестовой карте: ноль заклиниваний, ноль залипаний в углах | **arrived = 200, wedges = 0, cornerSticks = 0** — прогнано **независимым харнессом оркестратора**, свои пары, свои определения; худший прогон 606 шагов |
| ⑦ | `capture` непустой; `imagediff` против эталона, **просмотренного глазами** | `nonBlankPixels=921600/921600`, `diffPixels=0`; **кадр просмотрен оркестратором** — зелёная плоскость земли и светло-бежевая капсула по центру, читается |
| ⑧ | `12.D07` — детерминизм при переменном темпе кадров | проходит (600×1 / 300×2 / 100×6 дают одинаковое состояние) |

**Про пункт ⑦ отдельно.** `ORCHESTRATOR_PROMPT_M1.md` §7 предупреждал, что
«M1 adds a test map and obstacles, so the frame **will** change». Он **не
изменился**, и это корректно: тестовая карта WRLD-3 — data-модуль для тестов,
а не загружаемая зона, `enterZone` её не грузит. Благословлять новый эталон
не потребовалось.

**Что игра умеет сегодня:** всё, что умела после M0, плюс полноценная
навигация — сетка 0.5 м с областями связности, A\* с кольцевым бюджетом,
string-pull сглаживание, поле потока к цели, `snap` с null-контрактом,
жизненный цикл зоны с контрактным порядком эмиссии, и игрок, который
проходит тестовую карту с проёмом, колонной, вогнутым карманом и диагональным
углом — двести раз подряд, ни разу не застряв.

**Дальше — M2, и он не начинается без прямой команды владельца.**

**Урок этапа, стоит помнить при написании будущих «Done when»:** критерий
`12.P09` («худшее решение ≤ 1 мс») выполнялся у NAV-2 **за счёт отказа от
работы** — солвер укладывался в миллисекунду именно потому, что абортил по
потолку узлов. Временной гейт аборт прячет. Всюду, где критерий меряет время,
нужен парный критерий на **проделанную работу** (раскрытые узлы, найденные
пути, пройденные ячейки).

**Долги, назначенные поимённо, чтобы не потерялись:**
- **WRLD-3** — обязан починить **O-38** (`enterZone` отдаёт `nav` футпринты
  прошлой зоны) и учесть **O-37** (двойной `physics.rebuild`: проверять
  порядок событий, а не счётчик вызовов).
- **TEST-4** — обязан починить **O-34** (флейки под конкурентным раннером;
  без этого пункт ② гейта M1 недостижим) и включить **O-29** (полный режим
  `check-imports` для `src/physics/`, `src/actors/`, плюс `src/world/`,
  `src/nav/` как корни).
- Тикет с первым настоящим генератором `world` — **O-36** (взять
  `ctx.rng.fork()` и в том же заходе переписать `tests/core/boot.test.js`).

**Два внеплановых точечных ремонта принятого кода закрыты 2026-07-29**
(одобрены владельцем): **O-32** в `src/physics/cast.js` и **O-33** в
`src/actors/pool.js` — обе строки в таблице принятых ниже.

Критический путь: `WRLD-1 ✅ → WRLD-2 → NAV-1 → NAV-2 → NAV-3 → PLYR-2`.
**Строго по одному тикету за раз:** параллельный запуск двух
сабагентов был опробован на паре O-32/O-33 (файлы не пересекались, и это
сработало), но он же поднял частоту флейка O-34 с ~1/9 до 2/4 и испортил замер
«несколько зелёных прогонов подряд» — на тикетах M1, которые делят
`src/world/index.js` и `src/nav/index.js`, так делать нельзя тем более.

**Предупреждение о гейте:** пункт ② («`npm test` зелёный, несколько прогонов
подряд») сегодня **недостижим на полном наборе** — два теста флейкуют под
конкурентной нагрузкой раннера, см. **O-34**. Это харнесс, не продакшн-код,
но M1 закрывать с этим нельзя.

**Поправка к состоянию репозитория (2026-07-28, проверено оркестратором).**
Предыдущая редакция этой шапки и `ORCHESTRATOR_PROMPT_M1.md` §1 утверждают
«ничего не закоммичено». Это **неверно**: `git ls-files src/ tests/ tools/`
даёт 49 файлов — весь M0 закоммичен (последний коммит `f7414e4`). Незакоммичены
только PHYS-3, PHYS-4, PHYS-5 (`src/physics/{cast,separate,sweep}.js`,
`tests/physics/phys{3,4,5}.test.js`, правки `src/physics/index.js`).
Запрет на `git stash`/`checkout`/`restore`/`clean`/`reset` **остаётся в силе** —
радиус поражения меньше, чем считалось, но три принятых тикета всё ещё
восстанавливать неоткуда.

Команда владельца на старт M1 получена 2026-07-28; стоп-правило ниже
удовлетворено. Критический путь M1: `WRLD-1 → WRLD-2 → NAV-1 → NAV-2 → NAV-3 →
PLYR-2`; весь этап идёт **последовательно**, по одному сабагенту за раз
(`BACKLOG.md` §Scheduling: «M0–M1 — критический путь, параллельных дорожек нет»),
и дополнительно потому, что `NAV-1`/`NAV-5` делят `src/nav/index.js`, а
`WRLD-1`/`WRLD-3` — `src/world/index.js`.

Гейт прогнан оркестратором 2026-07-28:

| # | Пункт гейта | Результат |
|---|---|---|
| ① | `npm run build` | зелёный |
| ② | `npm test` — **пять прогонов подряд** | **379/379** каждый раз (гонка O-24 не воспроизводится) |
| ③ | `check-imports` (`roots=3`) + `check-fixed` (`bodies=1`) | PASS / PASS |
| ④ | `capture.mjs --shot boot_clean` | `nonBlankPixels=921600/921600`, PASS |
| ⑤ | `imagediff` против благословлённого эталона | `diffPixels=0`, PASS |
| ⑥ | `12.D07` на пустом мире | проходит |
| ⑦ | **живая игра в браузере** | клик → капсула прошла **4.83 м**, перенацелилась → **6.51 м**, камера следует, ноль ошибок |

Что игра умеет сегодня: поднимается в браузере на 1280×720, показывает плоскость
земли и капсулу под камерой 3/4 (FOV 35°, pitch 52°, дистанция 22 м); капсула
идёт в точку клика через `actors.moveTo` → `physics.moveBody` со скольжением
вдоль стен и шагом на уступ 0.45 м; камера следует с мёртвой зоной; потеря
контекста WebGL переживается и картинка возвращается.

**Дальше — M1, и он не начинается без прямой команды владельца** (стоп-правило ниже).

Критический путь M0 был: `CORE-1 → CORE-2 → CORE-3 → CORE-9 → RNDR-1`.

## Правила производительности, найденные измерением

Все найдены измерением на этой сборке Node, все стоили кому-то переписывания:

- **`Map` течёт при вставке никогда не повторяющегося ключа.** ACTR-1 держал
  `Map<id, Actor>` с монотонным счётчиком и `delete` при возврате слота — это
  дало **стабильные ~456 Б/вызов** на всех 5000 раундах (не шум): V8 компактит
  backing store даже когда живых записей не больше одной. Лечится арифметикой
  индексов вместо `Map`. Тот же класс находки, что у PHYS-1 в `grid.js`.
- **`array.length = 0` рвёт backing store массива** — следующая запись
  переаллоцирует. ACTR-1 усекал так плотный список живых на каждом `release()`;
  переписал на вычисляемый `Proxy`-вид над хранилищем, вообще без массива.
- **`Map.prototype.clear()` аллоцирует безусловно**, даже на пустой карте.
  Поэтому `actor.cooldowns` при рецикле слота **не очищается** — и это
  **незакрытый пробел**: как только `skills`/ACTR-9/ACTR-10 начнут туда писать,
  переиспользованный слот унаследует чужие кулдауны. Нужна структура на
  параллельных массивах с ручным счётчиком, а не `Map`. Владелец — ACTR-9/ACTR-10.
- **`Math.hypot` аллоцирует.** Проверено оркестратором независимо на этой сборке
  Node: `Math.hypot(x, y)` — **5.73 байта на вызов**, `Math.sqrt(x*x + y*y)` —
  **0.34**. Нашёл PHYS-2, поймав это в `moveBody`. В любом коде, помеченном
  `Alloc = no` (а это почти вся математика физики, боя и навигации),
  `Math.hypot` использовать **нельзя** — только `Math.sqrt`.

## Проверенные возможности окружения

Проверено оркестратором 2026-07-28 живым запуском, не предположено:

- **headless Chromium отдаёт WebGL2** — `vite preview` + Playwright, `canvas.getContext('webgl2')`
  возвращает контекст, кадр читается через `gl.readPixels`. Это снимает главный
  риск M0: `tools/capture.mjs` (TEST-3) сможет снять `boot_clean`, и вся
  B-поверхность харнесса (`baseline`, `profile`, `playtest`) выполнима.
  Запуск делался с `--use-gl=angle --enable-unsafe-swiftshader`; если в CI кадр
  окажется пустым — начинать надо с этих флагов.
- Playwright уже стоит из `devDependencies` (CORE-1), браузер скачан.

**Рецепт съёмки кадра — воспроизвести за минуты, когда дойдут руки до TEST-3:**

```
npm run build && npx vite preview --port 4321 --strictPort   # dist как статика
```
затем Playwright: `chromium.launch({ args: ['--use-gl=angle', '--use-angle=default',
'--enable-unsafe-swiftshader'] })`, `newPage({ viewport: { width: 1280, height: 720 } })`
— **1280×720, это разрешение пиксельного базлайна из `12-testing.md` §6**;
`page.goto(url)`; дождаться `window.__READY__ === true` (его выставляет B13);
дальше кадр гнать **только** через `window.__ENGINE__.frame(dt)` с фиксированным
шагом (lockstep), не через rAF, иначе съёмка недетерминирована; читать пиксели
через `gl.readPixels` либо `page.screenshot`.
Потеря контекста проверяется так же: `gl.getExtension('WEBGL_lose_context')`,
`loseContext()` и `restoreContext()` **обязательно из одного и того же
`page.evaluate`** — из разных вызовов восстановление не срабатывает.

## Правило работы с git для сабагентов

Помимо запрета коммитов и пушей: **`git stash` в этом проекте запрещён тоже.**

Причина конкретная, а не формальная. Весь результат тринадцати принятых тикетов
живёт **в рабочем дереве без единого коммита** — восстанавливать нечего и
неоткуда. `git stash` без `-u` захватывает трекнутые файлы **чужих** тикетов;
один сабагент (TEST-2) так и сделал, зацепив `docs/spec/02-api-contracts.md` с
правками RNDR-1 и RNDR-2, и вернул его через `stash pop`. Проверено: файл цел,
ровно три вставки, стек `stash` пуст, потерь нет. Но при обрыве соединения между
`stash` и `pop` (а обрывы в этой сессии уже случались) три строки контракта
исчезли бы молча.

Формулировка для брифов: **никаких `git stash`, `checkout`, `restore`, `clean`,
`reset` — вообще ничего, что трогает рабочее дерево целиком.** Нужно временно
убрать свой файл — переименуй его, а не прячь дерево.

## Стоп-правило владельца

**После закрытия M0 работа останавливается.** M1 не начинается без прямой
команды владельца проекта в текущем сообщении. Указание дано 2026-07-28.
Доложить результат гейта M0 и ждать.

## Контекст исполнения

- Исходный репозиторий `mshumer/Claude-of-Duty` **локально недоступен**. Тикеты,
  описанные в бэклоге как «Port …», реализуются по спецификации
  (`docs/spec/02-api-contracts.md`, `docs/ARCHITECTURE.md`), а не переносом кода.
- Коммиты не делаются. Изменения остаются в рабочем дереве до отдельного
  разрешения владельца.

## Закрытые тикеты

| ID | Дата | Файлы | Проверено чем | Заметки |
|---|---|---|---|---|
| PLYR-2 | 2026-07-29 (1 круг) | `src/player/move.js`, `src/player/index.js` (вайринг в PLYR-1), `tests/player/plyr2.test.js` | `npm run build` зелёный; `tests/player/` 41/41; `npm test` обе стадии зелёные; `lint` PASS/PASS; `capture` → 921600/921600; `imagediff` → `diffPixels=0`; **независимая проба оркестратора — СОБСТВЕННЫЙ харнесс, собственные пары старт/цель, собственные определения отказов**: сначала подтверждено, что `nav.version = 1 > 0`, то есть прогоны идут по **новому** пайплайну, а не по откату PLYR-1; 400 проходимых точек, из них **200 достижимых пар** с разносом ≥ 8 м (`nav.connected` истинно для каждой); результат — **arrived = 200, wedges = 0, cornerSticks = 0**, худший прогон 606 шагов (~10 с при 60 Гц). Определения: *заклинивание* — не дошёл за 3000 шагов при достижимой цели; *залипание в углу* — суммарное перемещение < 0.15 м за 180 подряд шагов без прибытия. Игрок ставился в стартовую точку **только через `actors.teleport`** (санкционированный путь), никаких прямых записей `actor.x/z` — dev-страж ACTR-2 их бы поймал | **Последний тикет M1 и пункт ⑥ гейта.** Реализованы все двенадцать шагов `11-flows.md` §3.4. **Шаг 3 (`nav.raycastNav`) реализован, но тёмный:** метод есть в таблице `02` §6, но его никто в M1 не построил, а импортировать `src/nav/smooth.js`'s `segmentWalkable` запрещает hard rule 2 — поэтому шаг закрыт `typeof`-гардом и каждый приказ уходит на шаг 4 (реальный запрос пути). Это осознанно: пропуск оптимизации заклинивания вызвать не может, а тихое «линия свободна» — может. **Найден и исправлен настоящий баг заклинивания при отладке:** `_repathFromCurrent` проверял `nav.connected()` от **сырой** позиции тела, а тело, только что вытолкнутое физикой из стены, может округлиться в непроходимую ячейку (растр 0.5 м грубее точного коллайдера) — `connected()` ложно давал false и **достижимый приказ отбрасывался**. Лечится снапом точки отправления **до** проверки связности. Второй баг — off-by-one в лимите повторов бюджета (`<=` пропускал тринадцатый запрос). **Совместимость с PLYR-1:** полный пайплайн включается только при `nav.version > 0`; при `version === 0` (бут без `enterZone`, как в `plyr1.test.js`) работает прежний прямой ход — поэтому все 24 теста PLYR-1 проходят без правок. **Найдено и не чинилось (чужой файл):** `nav.stats.refusals` растёт только на неудачном решении A\*, но не на отказе бюджета в `requestPath`, тогда как §3.4 шаг 5 пишет «`nav.stats.refusals` records it» — расхождение контракта с реализацией NAV-2 |
| TEST-4 | 2026-07-29 (1 круг) | `tests/nav/nav_perf.test.js`, `tools/check-imports.mjs`, `tests/tools/check-imports.test.js`, `package.json` (только `scripts`) | **`npm test` — ПЯТЬ ЗЕЛЁНЫХ ПРОГОНОВ ПОДРЯД, прогнано оркестратором собственноручно**, каждый 564 + 75 = **639 тестов**, обе стадии `fail=0`; `lint` PASS/PASS, **корней стало 6** (было 3), 26 файлов; `build` зелёный; `imagediff` → `diffPixels=0` | **Закрывает O-34 — единственный блокер пункта ② гейта M1.** Прогон разделён **по путям**, а не фильтром по имени (O-28 уже показал, что `--test-name-pattern` не останавливает побочные эффекты уровня файла — `capture.test.js` поднимает `vite preview` + Playwright при загрузке модуля). `npm test` = `test:unit && test:perf`, поэтому **ни один тест не стал недостижим** по умолчанию: `test:unit` перечисляет всё, исключая чувствительные файлы по пути, `test:perf` гоняет исключённые с `--test-concurrency=1`. **Ни один порог не ослаблен и ретраев не добавлено** — оба запрета прямые (`12-testing.md` P2; O-21 и O-24: «чинить изоляцией, никогда ретраями»). Чувствительными признаны шесть файлов: `phys4` (бюджет 0.4 мс), `alloc` (GC-тайминг), `capture` (браузер), `nav4` (p99 поля потока), собственный `nav_perf`, и — **сверх списка, найдено самим TEST-4** — `nav2.test.js`, чей тест `12.P09` меряет **максимум** по 30 сэмплам и в конкурентном прогоне давал 2.14 мс при гейте 1.0; править чужой файл он не стал, а перенёс в изолированную стадию. **Гейтовые ассерты написаны с защитой от ловушки NAV-2:** `12.P09` меряется на **решаемом** лабиринте (800 открытых ячеек, ниже потолка 1200) и каждый сэмпл проверяет, что путь **реально найден** (`pathLength ≥ 2`) и что `stats.refusals` **не вырос** — то есть таймиг не может быть удовлетворён отказом от работы. Числа (p99, не максимум; 300 сэмплов после прогрева): **`12.P08` p50 ≈ 0.84, p99 ≈ 0.91–1.14 мс** при бюджете 3.0; **`12.P09` p50 ≈ 0.02–0.03, p99 ≈ 0.04–0.045 мс** при бюджете 1.0. **O-29 выполнен на три четверти:** `src/actors/` переведён в полный N-режим, `src/world/` добавлен корнем в полном режиме, `src/nav/` уже был полным — все три чисты, проверено, а не предположено. **`src/physics/` оставлен как был** — см. **O-40**: он реально читает `performance.now()` в `separate.js`, переключение немедленно валит линтер, и сабагент правильно остановился вместо правки чужого принятого файла. **O-24 подтверждён закрытым** ранее через `--root`, **O-28 закрывается этим разделением** |
| WRLD-3 | 2026-07-29 (1 круг) | `src/world/index.js` (порядок + фикс O-38), `src/world/testmap.js`, `tests/world/wrld3.test.js` | `npm run build` зелёный; изолированно `tests/world/` 66/66; `lint` PASS/PASS; `capture` → 921600/921600; **`imagediff` → `diffPixels=0`** (тестовая карта не становится загружаемой зоной, кадр не изменился — благословлять нечего); **независимая проба оркестратора — 15 проверок, все зелёные**: **критерий выполнен** — лог слушателя на первом входе `zone:enter → physics.rebuild → nav.rebuild → physics.rebuild → zone:ready`, на смене зоны `zone:teardown → …` то же самое; `zone:teardown` **не** возникает на первом входе (предыдущей зоны нет) и стоит первым на смене; **O-38 доказан поведенчески, а не чтением кода** — сетка `nav` после 60-метрового `last_bastion` = **120×120**, после 112-метрового `bonereach` = **224×224**, каждая под свою зону, и `nav.walkable(50, 0)` после входа в `bonereach` = `true`, что при старом дефекте было невозможно (сетка держала бы границы ±30 м прошлой зоны); `staticFootprints` заморожены, `world.current` указывает на новую зону; **тестовая карта** — 12 футпринтов, виды только `box`/`cylinder`, поля `blocksNav`/`facing` (O-35 соблюдён) | Несёт фикс **O-38** — дефект принятого WRLD-1, который моя приёмка пропустила, потому что `nav` тогда не существовал. **Тест написан на порядок событий, а не на счётчик вызовов** — прямо по предупреждению O-37: `physics` рехеширует сетку второй раз собственным слушателем `zone:ready`, и ассерт «ровно один `physics.rebuild`» падал бы не по вине кода. Именно на этом я сам потерял круг ревью на WRLD-1. Карта: прямоугольник 40×24 м, две комнаты через **единственный проём шириной 2.0 м** (чокпоинт, а не свободное поле), внутри — свободностоящая колонна (обход), **U-образный вогнутый карман** (классическая ловушка-клин для PLYR-2) и **два ящика 2×2 м, касающиеся ровно одной диагональной точкой** (случай залипания в углу). Всё проходимое — **одна связная область** (`regionCount === 1`), между дальними углами (~41 м) реально решается `requestPath`, каждый узел пути проходим. Карта — самостоятельный data-модуль без импортов, а **не пятый `ZoneDescriptor`**: `zones.js` вне объёма тикета. Размеры проёма, стен, колонны, ящиков и кармана назначены и помечены — спека для рукотворной тестовой карты чисел не даёт |
| NAV-5 | 2026-07-29 (1 круг) | `src/nav/snap.js`, `src/nav/index.js` (метод `snap`), `tests/nav/nav5.test.js` | `npm run build` зелёный; изолированно `tests/nav/` **84/84**; `lint` PASS/PASS; `imagediff` → `diffPixels=0`; **независимая проба оркестратора — 18 проверок, все зелёные**: **null-контракт точен** — запрос в глубине блокера 12×12 м с радиусом 1.0 даёт ровно `null`, а не `undefined` (важно: потребители ветвятся на `=== null`), при достаточном радиусе находит проходимую точку; **инвариант I3 соблюдён буквально** — `snap(1.13, −2.41, 0.0)` возвращает `(1.13, −2.41)` координата-в-координату, а не центр ячейки, и `snap(blocked, 0)` даёт `null`; **«ближайшая» действительно ближайшая** — 400 запросов на захламлённой сетке сверены с **моим собственным** brute-force эталоном (клампинг запроса в прямоугольник каждой проходимой ячейки, код не общий с тикетом): **0 не-ближайших и 0 попавших на непроходимую ячейку, 0 расхождений по признаку «есть ли что-то в радиусе»**, худшее лишнее расстояние `1.00e-4` м — ровно задокументированный `CELL_EDGE_EPSILON`; детерминизм; `out` возвращается тем же объектом и совпадает с путём без `out`; **`null` не пишет в переданный `out`** (проверено отдельно — иначе вызывающий получил бы мусор при неудаче); вырожденный ввод не бросает: далеко за сеткой, отрицательный радиус, `NaN` — все дают `null`; **не аллоцирует** (0.0001 Б/вызов при N=1M) | **Критерий выполним только наполовину, и это зафиксировано честно:** «the three call sites branch on it» — из трёх названных `02` §6 потребителей (приказ на движение в заблокированную точку, разброс лута, установка портала) в M1 существует **один**: `actors.teleport` (ACTR-2) уже вызывает `nav.snap(x, z, TELEPORT_SNAP_RADIUS, out)` и ветвится на `=== null`. Сабагент прочитал `src/actors/motion.js`, **не редактируя его**, и подтвердил, что сигнатура садится в оставленный ACTR-2 seam; проверено отдельным тестом. Два других потребителя — `items` (не существует) и WRLD-4/порталы (M5). Заглушек не наделано. **Найден и починен до сдачи настоящий баг:** наивный клампинг в замкнутый интервал `[cellMin, cellMax]` мог вернуть точку ровно на **верхней** грани проходимой ячейки, а `cellIndexAt` по полуоткрытому соглашению относит такую точку к **следующей** ячейке, иногда заблокированной. Поймано собственным тестом «результат обязан быть проходимым»; лечится `CELL_EDGE_EPSILON = 1e-4` (0.1 мм, назначено), применяемым **только к уже выбранному победителю**, чтобы не смещать сравнение при ничьих. Тай-брейк — row-major (`cz` внешний, `cx` внутренний) со строгим `<`, поэтому равноудалённый поздний кандидат не вытесняет раннего. `out` — общая scratch-переменная (прецедент `astar.js`/`flow.js`), а не 32-кольцо `physics`: `snap` отдаёт ровно один `Vec3` за вызов, живых результатов одновременно не бывает. **Регионы сознательно не фильтруются:** `07` §12 I3 сам трактует `walkable(p)`, `regionAt(p) === entryRegion` и `snap(p,0)===p` как три **композируемые** проверки — встроить фильтр региона внутрь `snap` значило бы отнять эту композируемость без нужды хоть одному потребителю этого этапа |
| NAV-4 | 2026-07-29 (1 круг) | `src/nav/flow.js`, `src/nav/index.js` (вайринг `flowVersion`/`buildFlowField`/`flowAt`/`flowDistance`), `tests/nav/nav4.test.js` | `npm run build` зелёный; изолированно `tests/nav/` 67/67 (после удаления scope-тестов, O-39), `phys4` 14/14, `alloc` 5/5, `capture` 21/21; `lint` PASS/PASS; `imagediff` → `diffPixels=0`; **независимая проба оркестратора**: **`flowVersion` и `version` не смешаны** — 5 сборок двигают `flowVersion` ровно на 5 и не трогают `nav.version`, а `rebuild` двигает `version` и не трогает `flowVersion`; **экстент точно совпадает со спекой** — окно **квадратное** ±32 м (128×128), внутри **15876/15876** ячеек имеют конечное расстояние, снаружи **19964/19964** дают `Infinity`; **направления единичны** — худшее отклонение `1.11e-16` на всех ячейках окна; **проход по полю** — 181 старт со случайных проходимых точек на карте с 50 блокерами, **181 дошёл до цели, 0 застряли и 0 зациклились**; **стоимость действительно рулит**, а не маскирует: точка за патчем из 240 ячеек стоимостью 40 даёт `flowDistance` 20.38 м против прямой 14.00 м, то есть поле обошло дорогое место; **запечатанная камера внутри окна даёт `Infinity`** — поле не протекает сквозь стену; **детерминизм** — 14 400 ячеек, 0 расхождений между двумя независимыми экземплярами; `flowAt`/`flowDistance` **не аллоцируют** (0.0001 Б/операция при N=1M) | Bucketed monotone Dijkstra (Dial), `BUCKET_COUNT = round(COST_MAX·√2)+1 = 360` выведено из потолка стоимости, а не назначено. Очистка массивов заменена штампом поколения, где сам `flowVersion` и есть счётчик поколений — чистится только `_bucketHead` (~360 записей). Поле, пережившее `rebuild`, помечено записанной `_builtGridVersion`: пока она не совпадает с текущей `nav.version`, `flowAt`/`flowDistance` отдают документированные «недостижимо» (`{0,0}` / `Infinity`) и **не перечитывают сетку** на запросе, поэтому не могут прочитать устаревшую геометрию. Два расстояния сознательно разведены: целочисленный `priority` (взвешенный стоимостью, задаёт форму дерева) и `distM` (метры, без округления, то, что отдаёт `flowDistance`) — это примиряет «поле обязано уважать стоимость» с «`flowDistance` в метрах, а не в единицах стоимости». **Сабагент нашёл и починил у себя настоящий баг:** первая версия индексировала связный список корзин **по ячейке**, а не по событию релаксации; при decrease-key повторная релаксация затирала указатель, нужный ещё не осушённой корзине, и молча сиротила узлы — замер показал ~28 потерянных ячеек и тысячи навсегда `OPEN`. Исправлено списком по событию (`_eventCell`/`_eventNext`). **Методический вывод по замеру, важный для TEST-4:** первая формулировка критерия («перестройка ≤ 0.8 мс») меряется максимумом, а максимум на субмиллисекундной операции меряет планировщик ОС, а не код. Чистый замер оркестратора на 2000 сборках (400 блокеров + 2137 ячеек с поднятой стоимостью): **p50 0.4007 мс, p90 0.4346, p99 0.6165, p99.9 0.8441, max 3.1765**; бюджет превышают **0.15%** сборок. Модель `06` §9.1 даёт 0.295 мс — сходится. На той же конфигурации «худшее из 80» скакало от 0.53 до 3.07 мс между прогонами, то есть было чистым шумом. **Перф-критерии субмиллисекундных операций надо формулировать в перцентилях** |
| NAV-3 | 2026-07-29 (1 круг) | `src/nav/smooth.js`, `src/nav/index.js` (вайринг `smooth`), `tests/nav/nav3.test.js` | `npm run build` зелёный; `npm test` **594/594 начисто** (все три флейкующие группы O-34 в этом прогоне тоже прошли); изолированно `tests/nav/` 56/56, `phys4` 14/14, `alloc` 5/5, `capture` 21/21; `lint` PASS/PASS; `imagediff` → `diffPixels=0`; **независимая проба оркестратора — 16 проверок, все зелёные, и проходимость в ней меряется плотной выборкой прямо по `grid.flags`, а не через `segmentWalkable` тикета** (то есть не через проверяемый код): **обе половины критерия** — лестничный путь 81 → **2 узла**, каждый сегмент проходим по всей длине; **угловая ловушка** — блокер 6×6 м между концами: прямой отрезок старт→цель по независимой выборке **нелегален**, и сглаживание честно останавливается на **3 узлах**, а не схлопывается в 2 сквозь угол; **стресс — 171 сглаженный путь на захламлённых картах (60 коробок каждая), НОЛЬ сегментов, уходящих с проходимой земли**, при сокращении 13 981 → 712 узлов (**94.9%**); идемпотентность (второй проход ничего не меняет), сохранение первого и последнего узла, отсутствие падений на вырожденных путях; **детерминизм** — два независимых экземпляра дают узел-в-узел одинаковый результат | Классический string-pull: якорь, проба вперёд, коммит последнего хорошего узла. **Ключевое решение — тест проходимости отрезка.** `02` §6 объявляет `raycastNav` («grid-space line of walk»), но его никто в M1 не реализует, поэтому тест сделан приватно в `smooth.js` и **экспортирован** (`segmentWalkable`), чтобы будущий тикет `raycastNav` переиспользовал правило, а не вывел второе — расхождение двух правил линии проходимости это ровно тот класс, что уже стоил проекту двух внеплановых ремонтов (O-32, O-35). Обход — DDA Amanatides–Woo по параметру `t ∈ [0,1]`, **без** `Math.sqrt`/`Math.hypot` вообще. **Главная тонкость, которую сабагент разобрал верно:** узлы пути всегда центры ячеек, поэтому отрезок регулярно проходит **точно через угол решётки**, где `tMaxX` и `tMaxZ` совпадают; наивное продвижение по одной оси пропустило бы две из четырёх ячеек, сходящихся в этой точке — то есть дало бы ровно тот срез угла, ради предотвращения которого тикет существует. На каждой такой ничьей (`|tMaxX − tMaxZ| < 1e-9`, назначено) требуются проходимыми **обе** «угловые» соседки, тем же правилом, что `astar.js` применяет к одиночному диагональному шагу. Предохранитель по числу шагов DDA (`width+height+4`, назначено) падает **в безопасную сторону** — лишний оставленный узел, никогда не срез. Укорочение пути — перезапись `Float32Array` по месту и уменьшение целого `nodeCount`; `array.length = 0` не используется. Ширина капсулы в тесте отрезка **не** учитывается сознательно: дилатация 0.30 м уже вшита в то, какие ячейки считаются проходимыми (`07` §6.3), остаток 0.06 м гасит скольжение `physics.moveBody` |
| NAV-2 | 2026-07-29 (2 круга) | `src/nav/astar.js`, `src/nav/index.js` (вайринг 7 методов), `tests/nav/nav2.test.js` | `npm run build` зелёный; `npm test` **578 тестов, 577 прошли** (единственное падение — известный флейк PHYS-4, O-34); **изолированно всё чисто**: `tests/nav/` 40/40, `phys4` 14/14, `alloc` 5/5, `capture` 21/21; `lint` PASS/PASS; `capture` → 921600/921600; `imagediff` → `diffPixels=0`; **независимая проба оркестратора**: `requestPath` **действительно не блокирует** — пять вызовов за один шаг вернули `[64,63,62,61,0]` за **52 мкс суммарно**, а `pollPath` по всем четырём принятым до первого `fixedUpdate` дал `null` (прямое доказательство, что инлайн-решения нет); **среза угла нет** — две коробки 2×2 м, касающиеся только диагональным углом, путь через шов не пропускают, и каждый узел лежит на проходимой ячейке (именно это потом меряет критерий PLYR-2 «zero corner sticks»); недостижимая цель в запечатанной камере отбрасывается за **0.016 мс** без раскрытия узлов, через `regionAt`/`connected`; освобождённый `requestId` больше не разрешается (класс дефекта ACTR-1); **детерминизм** — два независимых экземпляра на карте из 40 коробок дают путь узел-в-узел идентичный; **`12.P09`** — худшее решение на серпантинном лабиринте 0.0013 мс при гейте 1.0 мс | Реализованы `requestPath`, `pollPath`, `cancelPath`, `releasePath`, `pathNode`, `pathLength`, `setBudget`. Числа из `06` §9.1: бюджет **4** решения за шаг, потолок **1200** раскрытых узлов, аборт считается в `stats.refusals`. Вместо очистки visited-массивов на каждое решение — штамп поколения (`cellGen[i] === gen`), иначе 36 864 ячейки чистились бы четырежды за шаг. Запрос, переживший `rebuild`, отбрасывается по сохранённой `requestVersion` и не считается отказом. **Круг 1 не принят — и это лучший пример за этап того, как критерий приёмки выполняется без выполнения работы.** `12.P09` требует «худшее решение ≤ 1 мс», и тикет честно давал 0.07–0.13 мс — **быстро именно потому, что сдавался** на потолке 1200 узлов. Замер оркестратора на **одной** коробке 4×4 м посреди открытого поля: раскрытия росли квадратично по длине пути — 61 / 211 / 402 / 572 / 912 при путях 21 / 41 / 61 / 81 / 121 узел (то есть 2.9× → 7.5×), а на 80 м уже аборт. На карте 96×96 хватало **десяти** мелких коробок, чтобы диагональ 84 м не решилась при `connected() === true` и проходимых концах; одна hazard-полоса стоимостью 13 (реальное значение из `07` §6.4) ломала прямой путь на 60 м. Временной гейт всё это пропускал. **Требование оркестратора по итогу: регрессия обязана проверять число РАСКРЫТИЙ, а не время** — временные гейты аборт прячут, счётчики раскрытий нет; такая регрессия добавлена. **Круг 2 — и моя гипотеза о причине оказалась неверной:** я предполагал рассогласование единиц `g`/`h` (эвристика в метрах против стоимости в ячейках), сабагент проверил и показал, что эвристика уже считалась в ячейках. Настоящая причина — вырождение ничьих: на равномерной стоимости любая перестановка одной и той же боковой коррекции даёт **тождественно равный** `f`, и допустимая эвристика обязана честно объявить их равными, из-за чего A\* вынужден раскрыть весь прямоугольник «насколько я уже сместился». **Принятое решение — взвешенный A\* (`f = g + 1.5·h`), сознательный и задокументированный отход от допустимости** (Pohl 1970, субоптимальность ограничена множителем). Вес 1.5 назначен эмпирически как наименьший из проверенных {1.0, 1.2, 1.3, 1.5}, дающий запас: на самом тяжёлом случае 1.3 давал 1178 из 1200 — впритык к обрыву, 1.5 даёт 696. **Проверено оркестратором отдельно, что качество пути не пострадало:** отношение раскрытий к длине теперь **падает** с ростом дистанции (2.19× → 1.25×) вместо роста; все конфигурации 0…120 коробок решаются (169 → 361 раскрытие); а на hazard-диске радиусом 3 м из 112 ячеек стоимостью 13 путь **обходит** диск за 81 узел при истинной стоимости 81, отклоняясь на 3.25 м — то есть выбрал обход по цене 1 вместо пересечения примерно за 153, ровно как предписывает `07` §6.4 («агент делает крюк вокруг Ash Wall»). `gScore` остаётся истинной стоимостью, вес влияет только на ключ кучи |
| NAV-1 | 2026-07-29 (1 круг) | `src/nav/index.js`, `src/nav/grid.js`, `tests/nav/nav1.test.js`, **+2 строки** в `src/main.js` (O-12) | `npm run build` зелёный; `npm test` **556/556** начисто; **изолированно все три чувствительные группы O-34 чисты** (`phys4` 14/14, `alloc` 5/5, `capture` 21/21) плюс `tests/nav/` 18/18; `lint` PASS/PASS; `capture` PASS; `imagediff` → `diffPixels=0`; **независимая проба оркестратора — 26 проверок, все зелёные**: **`NAV_FLAG` существует в единственном экземпляре** — объект из `src/nav/grid.js` идентичен (`===`) объекту из `src/world/raster.js`, все семь битов совпали с `01` §9.3; **обе половины критерия**: стена, рассекающая карту, даёт разные области слева и справа (0 и 1), две точки на одной стороне — одну и ту же, ячейка внутри стены — `-1` и `walkable === false`, `connected()` ложно поперёк стены и истинно вдоль; **та же стена с проёмом 3 м снова соединяет стороны**; **версия строго растёт** (3→4→5→6), **счётчик процесс-глобальный** — второй экземпляр `NavSystem` продолжил с 7, а не начал с 1 (это ровно то, на чём держится `brain.pathVersion !== nav.version` при смене зоны), номера не переиспользуются, payload `nav:rebuilt` совпадает с `nav.version` на каждом вызове; **аксессоры не аллоцируют** — 0.0001 Б/операция при N=1M (`02` §6 помечает их `Alloc = no`); запрос далеко за границами сетки не бросает и честно отдаёт `walkable=false`, `region=-1`; **`nav.rebuild` в установившемся режиме — 1.291 мс при бюджете `12.P08` 3.0 мс** | Реализованы `grid`, `version`, `rebuild(zone)`, `walkable`, `flagsAt`, `regionAt`, `connected`, `stats`, N11 и событие `nav:rebuilt`. Остальные ~17 методов `02` §6 **отсутствуют, а не заглушены** — `snap` оставлен NAV-5 (он делит с этим тикетом `src/nav/index.js`), пути — NAV-2/NAV-3, поле потока — NAV-4. **Именованное разрешение оркестратора:** этому тикету позволено импортировать `src/world/raster.js` вопреки hard rule 2 — файл является **чистой библиотекой** (ноль импортов, ни `ctx`, ни состояния), а дублировать растеризатор в `src/nav/` было бы строго хуже; импорт локализован в одном файле `src/nav/grid.js`, `index.js` в `../world/` не заглядывает вовсе. **`NAV_FLAG` переприсвоен реэкспортом, а не копией** — по требованию брифа: две копии, способные разойтись, это ровно тот класс дефекта, что уже стоил проекту двух внеплановых ремонтов (O-32, O-35). **Разрешена ещё одна наименовательная нестыковка спек:** `07` §6.1 пишет, что `nav.rebuild(zone)` читает `zone.nav.height`, но в `01` §9.3 поле `height` — это **целое число ячеек**, а массив высот называется `groundY` (`Float32Array`). Решено в пользу `01` — тем же способом и по тому же прецеденту, что O-35 для `Footprint`. **Найдено этим тикетом и вынесено в O-38:** `world.enterZone` зовёт `nav.rebuild()` без аргумента и **до** обновления `_current`/`_staticFootprints`, то есть отдаёт футпринты прошлой зоны; подтверждено оркестратором чтением строк, назначено WRLD-3. NAV-1 со своей стороны сделал вызов неломающимся — без зоны деградирует до документированной заглушки 2×2 м. **Холодный `rebuild` — 6.6–7.3 мс** против 1.29 мс в установившемся режиме; помечено для первой загрузки зоны |
| WRLD-2 | 2026-07-29 (1 круг) | `src/world/raster.js`, `tests/world/wrld2.test.js` | `npm run build` зелёный; `npm test` **538/538** на чистом прогоне (два других — только флейк O-34, изолированно `phys4` даёт 14/14 шесть прогонов подряд); `lint` PASS/PASS; `capture` → 921600/921600; `imagediff` → `diffPixels=0`; **независимая проба оркестратора — 27 проверок, все зелёные, и модуль в ней импортируется в голом Node без `PhysicsSystem`, без `three`, без DOM** (это половина критерия): **биты `NAV_FLAG` сверены с `01-data-model.md` §9.3 по одному** — все семь совпали, объект заморожен; сетка 192×192 = **36 864 ячейки**, `flags`/`cost` — `Uint8Array`, **`region` — `Int16Array`** (знаковый, иначе `-1` не выразить), `groundY` — `Float32Array`; формула индекса проверена по осям (`cz*width+cx`, шаг по Z добавляет `width`); **прогнан собственный acceptance-фикстур спеки (`07` §12 шаг 2)** — комната 20×20 м с проёмом 3 м и колонной: интерьер даёт **ровно одну** связную область, то есть проём остался проходим; `blocksNav:false` блокирует **0 ячеек** (прямая проверка, что читается верное имя поля, а не `navBlock`); **детерминизм** — два прогона на 300 футпринтах дают побайтово идентичные `flags`/`cost`/`region` и одинаковый `regionCount`; **N10** — обнесённый стенами карман 1.5×1.5 м вырезан, `region` пруненой ячейки = `-1`; **стоимость** — 255 внутри блокера, 1 на открытом полу; **дилатация измерена напрямую** на большом axis-aligned боксе: заблокированная оболочка выходит на **0.250 м** за грань, следующий центр ячейки лёг бы на 0.75 — порог между ними, что соответствует 0.30 м; **замер оркестратора: полная растеризация 192×192 на 1150 футпринтах — 1.402 мс при бюджете `12.P08` 3.0 мс** | Чистый модуль: **ноль импортов**, ни `three`, ни подсистем, ни `ctx`, ни состояния между вызовами — поэтому тривиально переносим, если владелец позже решит, что файл принадлежит `nav`. **Расхождение путей разрешено оркестратором:** `07` §12 шаг 2 называет файл `src/nav/raster.js`, бэклог — `src/world/raster.js`; взят путь бэклога (он управляет объёмом тикета, тот же прецедент, что на WRLD-1), потому что `WRLD`-тикет не вправе писать в `src/nav/`, а обратный вариант заставил бы NAV-1 импортировать модуль чужой подсистемы вопреки hard rule 2. Чистота модуля делает это решение обратимым без потерь. Реализованы N1–N10; **N11 сознательно не реализован** — ему нужны процесс-глобальный счётчик версий и шина событий, это NAV-1. **`groundRegions` и `height` ещё не существуют** (WRLD-1 не строил `height.js`), поэтому оба входа опциональны: без ground-регионов N2 штампует всю сетку `walkable`, а N4 вырезает из неё реальные дыры — именно это и делает возможным критерий «from footprints alone»; N3 без поля высот — честный no-op, а не сфабрикованное нулевое поле. **`NAV_FLAG` транскрибирован локально** (вариант «а»), помечен как транскрипция, которую NAV-1 вправе переприсвоить: `02` §6 отдаёт `NAV_FLAG` подсистеме `nav`, но `src/nav/` ещё не существует, а писать туда этот тикет не вправе. **N10 после N7:** выжившие области переразмечаются в плотный диапазон `0..n-1` в том же row-major порядке первого появления, поэтому область 0 по-прежнему содержит ячейку с наименьшим индексом, а `regionCount` — настоящий послепруновый. **Ловушка O-35 в псевдокоде §6.3 обойдена** — она была выдана в бриф поимённо: спека даёт `stampFootprint` с полями `navBlock`/`rotation`/`'circle'` и вызовом `hypot`, то есть в копипастопригодном виде содержит и устаревшие имена, и запрещённую аллоцирующую функцию. **Холодный первый вызов ~10 мс** против 1.4 мс в установившемся режиме — помечено для NAV-1: если `nav.rebuild()` окажется на критическом пути первой загрузки зоны, он разово вылезет за бюджет, пока JIT не прогреется |
| WRLD-1 | 2026-07-29 (1 круг + один круг, потерянный по вине оркестратора — см. заметку) | `src/world/index.js`, `src/world/data/zones.js`, `tests/world/wrld1.test.js`, **+2 строки** в `src/main.js` (O-12) | `npm run build` зелёный; `npm test` **505/505** (падения на прогонах 2–3 — только известные флейки O-34, подтверждено изоляцией: `tests/tools/capture.test.js` даёт 21/21 три прогона подряд); `lint` PASS/PASS; `capture` → `nonBlankPixels=921600/921600`; `imagediff` → `diffPixels=0`; `src/world/` **не импортирует `three`** — Node-safe; **независимая проба оркестратора — 44 проверки, все зелёные**: **заморозка сломать не удалось шестью способами** — `push`, присваивание по индексу, запись в поле, `delete`, `Object.defineProperty`, `length = 0`, все шесть бросают; заморожен и массив, и **каждая запись**; к моменту `zone:ready` уже заморожен; **все четыре дескриптора сверены с собственной таблицей `01-data-model.md` §9.1 поле-в-поле** — `displayName`, `kind`, `sizeX`/`sizeZ`, `monsterLevel` (Last Bastion 60×60 town; Ashen Wastes 96×96 open, mlvl 6; Bonereach 112×112 dungeon, mlvl 11; Altar 48×48 arena, mlvl 15), значения `generator` — из вокабуляра спеки; **O-35 соблюдён точно** — виды легальны, записи несут `facing`, `y`+`height`, `blocksNav`, запрещённые имена встречаются только в шапке-комментарии, объясняющей правило; физика держит ровно столько статик, сколько опубликовал `world`; **утечки статик между входами нет** — шесть подряд `enterZone` в ту же зону оставляют `physics.stats.statics` = 4, без роста; `seedFor` детерминирован и реагирует на `zoneId`, `runIndex` и `worldSeed` по отдельности; `src/main.js` получил **ровно две строки** (проверено диффом) | Первый тикет критического пути. Реализованы `descriptor`, `descriptors`, `current`, `staticFootprints`, `seedFor`, `setWorldSeed`, `enterZone`; остальные ~18 методов `02` §5 **отсутствуют, а не заглушены** — они не нужны ни критерию, ни пути эмиссии. Геометрия сегодня — явно помеченная заглушка (четыре периметральных бокса из `descriptor.sizeX/sizeZ`, без RNG): генераторов ещё нет, они за пределами двухфайлового объёма тикета. Это осознанно: заглушка прогоняет полную форму записи O-35 и механику заморозки, а WRLD-3/6/9/10 заменят её целиком. **`07` §12 шаг 1 описывает более широкий результат** (`src/world/footprint.js`, `src/world/height.js`, выпуклые функции расстояния, поле террас) — не делалось, потому что колонка Files бэклога называет два файла, а его же правило гласит «тикет, которому нужен четвёртый файл, — это два тикета». Следствие: `groundHeight`/`surfaceAt` отсутствуют, а не аппроксимированы. **`ctx.rng.fork()` не берётся** — см. O-36: заглушечная геометрия случайности не требует, а форк ронял чужой `boot.test.js`. **Круг, потерянный по моей вине:** я отправил тикет на доработку, посчитав, что `physics.rebuild()` зовётся дважды из `world`. Счётчик вызовов говорит «сколько», но не «кто»; стек-трейс показал, что второй вызов идёт из **собственного слушателя `zone:ready` в PHYS-1** — то есть из принятого чужого тикета, см. **O-37**. Отправку отозвал, сабагент вернул код к исходному виду. Урок вынесен в O-37 |
| **O-32 fix** (ремонт PHYS-3) | 2026-07-29 (1 круг) | `src/physics/cast.js`, `tests/physics/phys3.test.js` (регрессии) | `npm run build` зелёный; `npm test` **482/482 на прогонах 1, 3, 5** (прогоны 2 и 4 — только известный флейк PHYS-4, O-34); `lint` PASS/PASS; `capture` → 921600/921600; `imagediff` → `diffPixels=0`; **независимая проба оркестратора — 7 проверок, все зелёные**: дефект снят — `circleCast` при `radius > 0` даёт **0 промахов на 5074** реальных пересечениях (было 5, 0.099%), при `radius 0` — 0 на 2790, как и раньше; **радиус 0 доказан не «неизменным», а корректным** — 4000 запросов `rayCast` сверены с **grid-free аналитическим эталоном** (slab-тест без всякой сетки): 0 расхождений, худшая дельта расстояния **3.55e-15**, то есть шум плавающей точки; **оригинальный критерий приёмки PHYS-3 перепрогнан на пропатченном файле** — `lineOfSight` против `rayCast` на **10 000 случайных пар**, 0 расхождений; **переполнение буфера кандидатов больше не теряет блокирующий статик** — 201 / 301 / 601 статик на пути, стена находится во всех трёх случаях, ровно на `x=4.650`; цена: `rayCast` (радиус 0) 0.320 µs/вызов, `circleCast` при радиусе 0.3 — **2.38 µs** в сцене из 800 статик на 96×96 м | Внеплановый ремонт принятого PHYS-3, одобрен владельцем 2026-07-29 (см. O-32). Перенос уже доказанного решения из `sweep.js` (PHYS-5): обход расширен до блока `(2·⌈radius/cellSize⌉+1)²` вокруг каждой ячейки центральной линии, при `radius = 0` вырождается в прежнюю одну ячейку — отсюда и бит-в-бит неизменность `rayCast`/`lineOfSight`, подтверждённая эталоном. **Дефект был невидим для критерия приёмки PHYS-3 по построению:** «`lineOfSight` сходится с `rayCast` на 10 000 пар» — оба вызова с радиусом 0, где дефекта нет. Это стоит помнить при написании будущих «Done when»: критерий, проверяющий только вырожденный случай, не проверяет метод. **Принято на собственной верификации оркестратора** — отчёт сабагента к моменту окончания проверки не поступил, но по регламенту §5.4 он и так является заявлением, а не фактом |
| **O-33 fix** (ремонт ACTR-1) | 2026-07-29 (1 круг) | `src/actors/pool.js` (`makeAllViewHandler`), `tests/actors/actr1.test.js` (+4 регрессии) | `npm run build` зелёный; `tests/actors/` **84/84**; `lint` PASS/PASS; `capture` → 921600/921600; **независимая проба оркестратора — 13 проверок, все зелёные**: индексные методы ожили (`slice`/`map` → `[0,1,2]`, `filter(Boolean)` → **3**, `forEach` вызывается **3 раза**, при том что `for..of`/`Array.from`/спред продолжают работать как раньше); **границы точны** — `2 in all` true, `3 in all` (ровно `_liveCount`) false, `-1` и `'1.5'` false; **подвижная граница проверена отдельно** — после `despawn` длина падает до 2, индекс освободившегося слота перестаёт числиться, `slice()` не отдаёт ни дыр, ни мёртвых записей; вид **остался read-only** (`set`/`delete`/`defineProperty` бросают); дескриптор `{writable:false, enumerable:true, configurable:true}`; чтение **не аллоцирует** — 0.0001 Б/операция при N=1M. **Главная проверка ради которой всё делалось:** `hashState` по живому `actors.all` теперь меняется на реальном `moveTo` и совпадает **до последней цифры** с хешем тех же акторов, переданных плоским массивом (436943825 → 363542116 по всем трём путям; было 1147091918 → 1147091918, то есть слепо) | Внеплановый ремонт принятого ACTR-1, одобрен владельцем 2026-07-29. Обе новые ловушки (`has`, `getOwnPropertyDescriptor`) делят один предикат `isLiveIndex()` с уже существовавшим `get` — три ловушки больше не могут разойтись по границам. `configurable: true` обязателен: цель `Proxy` — навсегда пустой массив, и не-configurable дескриптор для отсутствующего у цели свойства уронил бы инвариант `Proxy`. **`ownKeys` сознательно не реализован**, и обоснование принято: (а) по всему репо нет ни одного `Object.keys(actors.all)`, `JSON.stringify(actors.all)` или спреда вида в объект — только массивное потребление; (б) честный `ownKeys` обязан возвращать свежий массив строк-ключей на каждый вызов, то есть аллоцировать на 60-герцовом пути. Без ловушки запрос проваливается в `Reflect.ownKeys` пустой цели и отдаёт `['length']`, что безвредно. Материализовать вид в настоящий массив **запрещено** и не делалось — именно от этого ACTR-1 уходил (`array.length = 0` рвёт backing store) |
| ACTR-2 | 2026-07-29 (2 круга) | `src/actors/motion.js`, `src/actors/index.js` (вайринг 5 методов + окно записи вокруг `spawn`/`despawn`), `tests/actors/actr2.test.js` | `npm run build` зелёный; `npm test` **478/478 на трёх прогонах из пяти** (два падения — **чужие** флейки, см. O-34: acceptance PHYS-4 и `assertAllocationFree`; изолированно `tests/actors/` даёт **80/80 восемь прогонов подряд**); `lint` PASS/PASS; `capture` → `nonBlankPixels=921600/921600`, `imagediff` → `diffPixels=0`; **независимая проба оркестратора — 18 из 19 проверок зелёные** (единственный красный пункт — O-33 в чужом `pool.js`, не этот тикет): обе половины критерия — прямая запись `actor.x`/`actor.z` бросает **и значение не меняется** (проверял значение, не только бросок), `moveTo` через тот же страж работает; **замок не утекает при исключении** — принудительно уронил `pool.acquire()` внутри окна, страж остался взведён, посторонняя запись не прошла; **единицы `face` — радианы в секунду**, 180° при 720°/с заняли 14 шагов при предсказании 15 (чтение «за шаг» дало бы 1); **seam для NAV-5** — с отсутствующим `nav` не падает, при `nav.snap() → null` возвращает `false`, при успешном снапе возвращает `true` и садится на **снятую** точку (31, 31), а не на сырую; страж **выпадает из прод-сборки** — `installActorWriteGuard` встречается **0 раз** в обоих бандлах; цена аксессоров 0.166 µs/вызов, 56 акторов × 1 движение за шаг = **0.0093 мс/шаг**; **отдельная проба счётчика глубины**: несбалансированный `end` бросает, при вложенности 3 окно открыто после 2 из 3 `end` и взводится только на третьем, **5000 циклов spawn/despawn** не сдвигают счётчик — страж взведён, `moveTo` работает | Взято: `teleport`, `face`, `moveSpeed`, `distance`, `inRange` + страж. **`applyImpulse` сознательно не взят** и это обосновано: ему нужны `ActorsSystem.fixedUpdate()` для затухания импульса, поле «остаток импульса» и lockout состояния `knockback` (ACTR-9/10, не построены) — частичная версия, пишущая `velX/velZ` один раз и никогда не затухающая, дала бы ложный сигнал «работает». Помечено в `motion.js`. **Расхождение с прецедентом RNDR-2 намеренное:** камера защищена `Proxy`, потому что three мутирует приватные поля кватерниона мимо аксессоров; у `actor.x/z` — простые числа на простой записи, поэтому взяты пер-полевые `Object.defineProperty` с общим замком, ставятся лениво и идемпотентно через `WeakSet` при первой выдаче слота. **`begin/endActorWrite` — пара, а не колбэк `withActorWrite(fn)`:** `moveTo` может идти до раза на живого актора за шаг, колбэк аллоцировал бы там замыкание (у камеры окно открывается раз в кадр — другая частота). `spawn`/`despawn` обёрнуты окном не из-за расширения объёма, а потому что пул пишет `x/z` напрямую в `acquire`/`resetActorRecord`. **`moveSpeed` считает точную формулу `03-combat-math.md:270`, но возвращает сегодня 4.2 для всех** — ни таблицы архетипов (`classRunSpeed`), ни композиции статов (`movementSpeed`) ещё не существует; помечено как заглушка данных, не формулы. **`teleport` без `nav`** ставит актора в сырую точку и возвращает `true` — альтернатива (всегда `false` до NAV-5) сделала бы метод непригодным для уже написанного вызова `world.entry → actors.teleport`. **Круг 1 не принят:** окно записи было **булевым флагом, а не счётчиком глубины** — вложенный `end` закрывал внешнее окно, и легальный писатель после этого падал на собственной записи. Через публичную поверхность сегодня недостижимо, но обёрнутых путей уже четыре (`spawn`, `despawn`, `moveTo`, `teleport`), и первый же слушатель `actor:spawn`/`actor:death`, двигающий или удаляющий актора, это вскрыл бы. Круг 2 — счётчик глубины, `end` на нуле бросает громко |
| PHYS-5 | 2026-07-28 (2 круга) | `src/physics/sweep.js`, `src/physics/index.js` (вайринг `sweepProjectile`), `tests/physics/phys5.test.js` | `npm run build` зелёный; `npm test` **444/444 четыре прогона подряд**; `lint` PASS/PASS; `capture` → `nonBlankPixels=921600/921600`, `imagediff` → `diffPixels=0`; **независимая проба оркестратора — 14 проверок, все зелёные**: brute-force сверка с плотной выборкой (20 000 испытаний × 4000 сэмплов на сегмент, повёрнутые стены 0.2 м в случайной подъячеечной фазе, радиусы включая 0) → **0 туннелей**, 4105 реальных попаданий, 3 «лишних» срабатывания у углов (законно по D2); 401 статик на пути → блокирующая стена найдена, `x=4.900`; 60-метровый свип через ~30 ячеек → `x=29.9000`, `fraction=0.665000` точно; **5000 сверок с уже принятым `circleCast`** на одной геометрии → 0 расхождений; фигура из `11-flows` §5.5 (шаг 0.43 м против актора 0.36 м) → **0 туннелей на 122 пересекающих фазах**, `excludeId` уважается; аллокации (собственная проба, прогрев 6 млн итераций) — 0.049 Б/вызов при N=100k, ~0 при N=1M, **суммарные байты не растут** с N, `12.A01` держится. **Отдельная проба на радиусы больше ячейки** (сетка 2 м; радиусы 0.5 / 1.5 / 2.5 / 4.0 м, по 6000 испытаний) → 16 063 реальных пересечений, **0 туннелей** во всех четырёх режимах | Схема: honest time-of-impact — DDA Amanatides-Woo по центральной линии, узкая фаза стримингом. Чтение `(dx,dz)` как **полного смещения шага**, а не направления (у `sweepProjectile`, в отличие от `circleCast`, нет параметра `maxDist`; `11-flows` §5.5 шаг 4 сweeps «from the previous to the new position») — назначено сабагентом, проверено мной: `fraction = t / \|d\|` сходится. **Круг 1 не принят — два способа протуннелировать, оба найдены моей пробой, ни один не ловился его тестами:** (1) **сбор кандидатов шёл по линии луча, а запрос — диск**: статик, захешированный в соседнюю ячейку, до узкой фазы не доезжал вообще. При `radius = 0` невидим — поэтому ни его acceptance-тест, ни критерий PHYS-3 (оба радиуса 0) поймать это не могли; 4 туннеля на 20 000. Детерминированное репро: стена в 0.051 м от линии при радиусе 0.3 не находится, сдвиг линии на **0.002 м** через границу ячейки — находится, то есть ответ зависел от ячейки центра, а не от геометрии. (2) **буфер кандидатов `SWEEP_CANDIDATE_CAPACITY = 256` молча терял блокирующий статик**: 201 статик на пути — стена найдена, 261 — уже нет, `hit=false` без единого сигнала. Круг 2: обход расширен до блока `(2·⌈radius/cellSize⌉+1)²` вокруг каждой ячейки центральной линии (при `radius=0` вырождается в прежнюю одну ячейку), фиксированный буфер **удалён целиком** — узкая фаза теперь тестирует каждый найденный футпринт сразу, переполняться нечему. **Замеренная цена расширения (моя проба, 800 статик + 40 тел на 96×96 м):** `rayCast` 0.397 µs, свип при `radius=0` 0.376 µs, при `radius=0.3` — **1.141 µs (×3)**, 60-метровый свип 16.4 µs. 100 снарядов × 1 свип за шаг = **0.114 мс при бюджете S2 0.19 мс** (`11-flows` §5.5) — влезает, но это 60% всей стадии на одних свипах в плотной сцене; **обязательно к перемеру в SKIL-8/M4**, если снарядов станет заметно больше. `stats.casts` расширен: свип считается там же, что и касты — отдельного счётчика контракт не даёт, помечено в JSDoc. Новых строк в `02-api-contracts.md` не потребовалось: `sweepProjectile` уже был в таблице §4. Дефект (1) есть и в `circleCast` — **это чужой принятый тикет**, сабагенту трогать `cast.js` было запрещено явно, см. **O-32** |
| PHYS-4 | 2026-07-28 (1 круг) | `src/physics/separate.js`, `src/physics/index.js` (вайринг `separate` + реальный `stats.separationMs`), `tests/physics/phys4.test.js` | `npm run build` зелёный; `npm test` **424/424**; `lint` PASS/PASS; **независимая проба оркестратора — 12 проверок, все зелёные**: 5 сидов × 200 шагов = **1000 вызовов `separate(4)`**, p50 = 0.0071 мс, p99 = 0.0342 мс, **max = 0.0742 мс при бюджете 0.4 мс** (5× запас), максимальное перекрытие 0.64–0.79 м → **0.000000 м** на всех пяти сидах; **более плотная куча, чем в критерии** (56 тел — потолок `q.maxActors` — в диске 1.5 м, то есть вчетверо плотнее) тоже сходится в 0.000000 м; **два одинаковых мира расходятся побитово одинаково** (детерминизм); масса: лёгкое тело смещается ровно в **9.000** раз больше при массах 10 против 90, равные массы делят поправку 50/50 с точностью 1e-12; совпадающие центры **не дают NaN** и расталкиваются; `separate(0/-3/NaN/undefined)` — тихий no-op, не падение; `stats` — тот же объект, `separationMs` живой; аллокации: суммарно 15.6 / 9.8 / 13.3 КиБ при N = 10k / 100k / 500k — **не растут с числом вызовов**, среднее падает 1.60 → 0.027 Б/вызов | Схема: Гаусс-Зейдель, `iterations` полных проходов по всем парам. **O-30 закрыт замером, а не постройкой индекса:** телесный broadphase решено **не** строить — потолок `q.maxActors` = 56 даёт максимум 1540 пар, а замер даёт 5–50× запаса под бюджетом. В шапке `separate.js` помечено, что тикет, поднимающий `q.maxActors` существенно выше сегодняшнего потолка, обязан **перемерить**, а не унаследовать это решение. **Честная оговорка сабагента, подтверждённая моей пробой:** и он, и я греем JIT на выбрасываемой сцене перед замером; в непрогретом черновике **самый первый** в процессе вызов дал ~0.43 мс. Это стоимость первого вызова на холодном процессе, не установившийся режим — но если TEST-14/`profile.mjs` будет мерить холодный старт, число надо помнить. Решения D1–D5 в шапке `separate.js`: расталкивание чисто горизонтальное (высота и `flying` игнорируются, `flying` участвует наравне); `BODY_FLAG.NO_COLLIDE` исключается целиком (не толкает и не толкается); веса `wi = mj/(mi+mj)`, `mass <= 0` = бесконечная масса/неподвижное; совпадающие центры расходятся детерминированно по `+X`; `iterations <= 0` и нечисло — no-op, дробное усекается `Math.trunc`, верхнего клампа нет (бюджет — забота вызывающего `actors`). Прочтение «6 m circle» как **диаметра** (радиус 3 м) — назначено сабагентом и помечено: круг радиусом 6 м слишком разрежен, чтобы вообще нагрузить пасс |
| PHYS-3 | 2026-07-28 (1 круг) | `src/physics/cast.js`, `src/physics/index.js` (вайринг 7 методов), `tests/physics/phys3.test.js` | `npm run build` зелёный; `npm test` **410/410 три прогона подряд**; `lint` PASS/PASS; `capture` → `nonBlankPixels=921600/921600`, `imagediff` → `diffPixels=0`; **независимая поведенческая проба оркестратора — 37 проверок, все зелёные**: луч в стену даёт `x=4.000`, `distance=4`, `fraction=0.2`, нормаль `(-1,0)`; **`blocksSight:false` — рельса: `lineOfSight` проходит, `rayCast` при этом всё равно бьёт** (ровно то расщепление, которого требует `02` §4); кольцо `Hit` — **32 уникальных записи, 33-я переиспользует первую, переданный `out` кольцо не расходует**; `MASK.WORLD` видит только статику, `MASK.ACTORS` — только тела, `WORLD\|ACTORS` возвращает **ближайшее** из двух; толстый `circleCast` (0.9 м) останавливается там, где луч и тонкий кастер проходят в щель 1.0 м; конус/прямоугольник реагируют на `facing`; `overlapCircle` не пишет за `out.length`; `nearest` уважает `excludeId` и отдаёт 0 при пустом радиусе. **Независимый замер аллокаций оркестратором** (собственная проба, sink на `Float64Array`, калибровка пустой функцией 0.039 Б/вызов): при N = 10k → 1M суммарные байты держатся **0.5–2 МиБ и не растут**, а среднее падает 14.4 → **0.30 Б/запрос** — сигнатура O-23 (стабилизация inline-кэшей), не утечка; `12.A01` выдержан | Семь методов: `circleCast`, `rayCast`, `lineOfSight`, `overlapCircle`, `overlapCone`, `overlapRect`, `nearest`. `Hit`-кольцо (`HIT_RING_SIZE = 32`) заведено здесь — PHYS-2 оставил его «тикету, который введёт касты». Новых строк в `02-api-contracts.md` не потребовалось: все семь уже были в таблице §4. Сабагент прошёл ловушку O-23 сам: черновик давал стабильные ~16 Б/вызов на фиксированном N, разведён по **суммарным** байтам при росте N и вылечен **удлинением прогрева** (4–6 млн итераций), а не ослаблением порога. Решения, помеченные в шапке `cast.js` (D1–D8): касты плоские, `Footprint.y/height` игнорируется (вертикали нет ни в одной сигнатуре и в `Hit`); `circleCast` против box/poly инфлейтит полуплоскостями («срезанный угол»), то есть точен при `radius=0` и консервативно приблизителен на углах при `radius>0`; `(dx,dz)` нормализуется внутри; `BODY_FLAG.NO_COLLIDE` **не** выводит тело из запросов (он только про `moveBody`); попадание в тело даёт `surface:'flesh'`; `excludeId` сверяется с `actorId`, не с `bodyId`; `out` у overlap-методов не растёт — лишние совпадения молча отбрасываются; конус проверяет диск актора по радиусу, но только центр по углу (без `asin(r/d)`, чтобы не платить трансцендентной функцией за кандидата). Найдено сабагентом и вынесено в O-30: пространственного индекса по **телам** нет ни у кого — тела сканируются линейно |
| PLYR-1 | 2026-07-28 (2 круга) | `src/player/index.js`, `src/dev/debugview.js`, `tests/player/plyr1.test.js`, +2 строки в `src/main.js` | **Живая проверка оркестратором в браузере**: настоящий клик → капсула прошла **4.83 м** к первой точке, перенацелилась на второй клик и прошла **6.51 м**, камера следует, ноль ошибок на странице; `capture.mjs` → `nonBlankPixels=921600/921600`; **кадр просмотрен глазами** перед благословением эталона (зелёная плоскость + светло-бежевая капсула, читается); `imagediff` → `diffPixels=0` | «Один приказ за кадр»: `fixedUpdate` сверяет `intent.sequence` с последним потреблённым, поэтому кадр с шестью подшагами выдаёт **один** приказ, а шаг к цели идёт каждый подшаг. `fixedUpdate` не читает указатель — доказано тестом с Proxy, бросающим на `.input`. Движение — только через `actors.moveTo`, камера — только через `withCameraWrite`. Назначено и помечено: мёртвая зона камеры **1.5 м**; `MOVE_SPEED_MPS = 4.2` — не выдумка, это `runSpeed` Ravager из `01` §2. `renderX`/`renderZ` ещё не пишутся (`actors` не интерполирует до M2), поэтому `player` и `debugview` считают `lerp(prevX, x, alpha)` **локально**, не трогая запись актора — однострочная замена, когда `actors` начнёт писать. Игрок-плейсхолдер спавнится в `init()`, потому что `createCharacter` требует `items`/UI. **Круг 1 не принят:** отладочные меши были на `MeshStandardMaterial`, а света в сцене нет ни одного (`addLight` не реализован, `sky` — M8) → кадр **полностью чёрный**, `nonBlankPixels=0/921600`. Поймал пиксельный гейт; живая проверка кликом это пропустила, потому что смотрела на координаты, а не на пиксели. Круг 2 — `MeshBasicMaterial`, свет **не добавлялся** намеренно |
| ACTR-1 | 2026-07-28 (2 круга) | `src/actors/pool.js`, `src/actors/index.js`, `tests/actors/actr1.test.js`, +2 строки в `src/main.js` | `npm test` **339/339**, `lint` PASS, `build` PASS; **независимая проверка оркестратором четырёх сценариев**: удержание пулированной ссылки → бросает; алиасинг двух живых акторов → бросает; честный `out` на живом акторе → работает; честный `out` после переиспользования слота → `null` | `id = 1 + poolIndex + generation * capacity` — биекция, поэтому `id` действительно не переиспользуется, а `byId`/`resolve` декодируют слот обратной формулой **без `Map`**. **Круг 1 не принят:** `ref()` без `out` возвращал общий пулированный объект, и следующий вызов `ref()` затирал ранее сохранённую ссылку — устаревшая ссылка разрешалась в **чужого живого актора**. Оркестратор напоролся на это с первой попытки; тесты сабагента не ловили, потому что проверяли только путь с честным `out`. **Моё предложение (штамповать серийник в пулированный объект) сабагент обоснованно отверг:** `refA` и `refB` — один объект, серийник затирается вместе с содержимым, второй копии нет. Круг 2: `resolve()` бросает **по идентичности** с пулированным объектом, dev-only. Три находки про V8 — см. раздел правил производительности |
| PHYS-2 | 2026-07-28 | `src/physics/body.js`, `src/physics/index.js` (вайринг 5 методов), `tests/physics/phys2.test.js` | `npm test` **303/303**, `npm run lint` PASS, `npm run build` PASS; **независимая поведенческая проверка оркестратором**: тело по диагонали в стену упирается ровно в `x=0.700` (грань 1.0 − радиус 0.3) и проходит **6.000 м вдоль** стены — скользит, не залипает; уступ **0.45 м проходится**, 0.80 м блокирует; кольцо — ровно **32** уникальные записи, 33-я переиспользует первую, переданный `out` кольцо не расходует | Схема «move-then-depenetrate»: тело перемещается, затем до 4 итераций выталкивается по нормали самого глубокого пересечения. Тангенциальная составляющая не трогается — скольжение получается без явного вычисления касательной. Во внутреннем угле самое глубокое пересечение ищется заново каждую итерацию, поэтому толчок переключается между стенами и сходится в угол. Порог шага 0.45 м отсчитывается от внутреннего `_floorY` (назначено: `addBody` не принимает `y`). Назначенные числа помечены в коде: `FLY_ALTITUDE 2.2`, `MAX_RESOLVE_ITERATIONS 4`, `SKIN_WIDTH 1e-4`. `bodyId` переиспользуется через free-list — **обязательно к учёту в ACTR-2** |
| PHYS-1 | 2026-07-28 | `src/physics/index.js`, `src/physics/grid.js`, `tests/physics/phys1.test.js`, +2 строки в `src/main.js` | `npm test` **269/269**, `npm run lint` PASS, `npm run build` PASS; **независимый замер оркестратором**: хеширование 10 000 статик — медиана **0.935 мс**, худший из семи **1.633 мс**, порог 2 мс пройден даже по худшему; **`Footprint` сверен поле-в-поле** с `02` §4 — двенадцать полей, ни одного лишнего, дефолты совпадают; `LAYER`/`MASK` идентичны; модуль импортируется в Node, `three` в каталоге нет | Сетка 2 м, CSR-хранилище на `Int32Array` + собственная open-addressing таблица с Fibonacci-хешем вместо `Map`: первая версия на `Map` давала **2.5–3.6 мс и не проходила**, профилирование показало накладные расходы самого `Map`. `addStaticGroup` не импортирует `three` — читает `geometry.boundingBox` и `matrixWorld.elements` утиным типированием, поэтому каталог остаётся Node-safe. Детерминизм: обход строго по возрастанию хендла, внутри — по `cx`, затем `cz`; тест сверяет канонический дамп двух независимых инстансов. `groundHeight` — плоская заглушка по границам зоны (`0` внутри, `-Infinity` вне), потому что heightfield принадлежит `world`; помечено в JSDoc |
| TEST-3 | 2026-07-28 | `tools/capture.mjs`, `tools/imagediff.mjs`, `src/dev/shots.js`, `tests/tools/capture.test.js`, `tests/fixtures/shots/boot_clean.png`, `package.json` (scripts) | **Независимая проверка оркестратором:** `capture.mjs --shot boot_clean` → `nonBlankPixels=921600/921600`, 1280×720, код 0, 0.9 с; два прогона подряд **побайтово идентичны** (`md5` совпал); `imagediff` на копии с **одним** изменённым пикселем → `diffPixels=1`, код 1, на идентичных → 0 — нулевой допуск работает; `npm test` **242/242**, `npm run lint` зелёный | Кадры гонятся через `engine.frame(FIXED_DT)`, не через rAF. Непустота проверяется по-настоящему: скан всех пикселей на `alpha>0 && (r|g|b)>0`, а не «файл существует». `capture.mjs` сам поднимает `vite preview` на свободном порту и гасит его в `finally`; есть `--base-url` для уже поднятого. **Находка:** первая версия читала пиксели через `canvas.toDataURL()` и получала **пустой кадр** — `RenderSystem` (верно) не ставит `preserveDrawingBuffer`, и backbuffer успевает очиститься между вызовами `evaluate`. Решение — `locator('#game').screenshot()`. Заведены id `12.CAP01/02`, `12.IMG01/02` по прецеденту TEST-2 |
| TEST-2 | 2026-07-28 | `tools/check-imports.mjs`, `tools/check-fixed.mjs`, `tests/tools/*.test.js`, `package.json` (`lint:imports`, `lint:fixed`, `lint`) | `npm test` — **221/221**; `npm run build` зелёный; **независимая проверка оркестратором на живых нарушениях**: прямой `import three` в `src/combat/` → `FAIL 12.N01`, код 1; **транзитивная цепочка** `combat → shared/vecs → three` → поймана с печатью всего пути, код 1; `check-fixed` на пробе с `performance.now()`, `Math.random()` и `ctx.time.dt` внутри `fixedUpdate` → три `FAIL 12.F01` с номерами строк и id класса, код 1; после уборки оба линтера дают 0 | Разбор импортов — BFS по графу (статические, bare, `export … from`, динамические), с маскировкой комментариев и строк, чтобы проза про `three` в комментарии не считалась нарушением. **`src/core/` добавлен шестым корнем, но только на `three`**: `window`/`document` там не проверяются, потому что `config.js`/`input.js` легально читают их под `typeof x !== 'undefined'` — это их способ оставаться Node-safe. **Узкое исключение для `ctx.get/peek/has`** — иначе `materials.get` (Fixed=N) совпадал бы по имени с核 core-контрактом и линтер орал бы на каждый `ctx.get()` в каждой подсистеме. Единственный легальный `Math.random()` разрешён по паре условий: файл ровно `src/main.js` **и** маркер `11-flows.md §14.2` на той же строке. Границы `check-fixed` честно перечислены в отчёте (вычисляемый доступ, template-литералы, совпадение по имени метода без учёта получателя) |
| TEST-1 | 2026-07-28 (2 круга) | `tests/helpers/{hash,alloc,actor,seed}.js` + тесты на них, `package.json` (scripts) | `npm test` — **199/199** (O-2 закрыт: скрипт приведён к глобу `node --expose-gc --test "tests/**/*.test.js"`, `--expose-gc` вшит, чтобы проба `12.A03` не пропускалась молча); **сверка хеша оркестратором**: детерминирован, порядок акторов канонизирован, `step` и позиция rng-потока входят, презентационные поля (`renderX`, `facing`) не влияют; **после круга 2** — каждое из девяти симуляционных полей (`x, z, life, mana, rage, resonance, stamina, state, actionSeq`) меняет хеш, посторонняя строка `secondary` — нет | Хелперы построены так, чтобы появление настоящих `actors`/`items` не потребовало переписывания: `hashState` принимает снимок `{actors, items, rngStreams, step}`, а не лезет в `ctx.get('actors')`. Исключение презентации — **по построению**: проекция читает жёстко заданный массив имён полей, в файле нет ни `JSON.stringify`, ни spread, ни `Object.entries`. `allocatedBytes` взят из `12-testing.md` §4.4 дословно; `assertAllocationFree` проверен **в обе стороны** — есть тест, что он бросает на аллоцирующей функции. **Круг 2:** хеш переведён со слота `secondary` на числовые ресурсы, см. D-5 |
| RNDR-3 | 2026-07-28 (2 круга) | `src/render/index.js` (дополнение), `tests/render/context-loss.test.js` | `npm run build`; весь набор **199/199**; **живая проверка оркестратором настоящим `WEBGL_lose_context` в headless Chromium**: `preventDefault` вызван (`defaultPrevented=true` у последующего слушателя), эмитятся `lost` → `restored`, `time.scale` **1 → 0 → 1**, `gl.isContextLost()` возвращается в `false`, `frame()` при мёртвом контексте не бросает, **пиксель после восстановления `[65,75,90,255]` — идентичен исходному**, ошибок на странице нет | `time.scale` выставляет **`render`**, а не движок: диаграмма §13.4 рисует это в дорожке `engine`, но `engine.js` принят и заблокирован. Восстанавливается **прежнее** значение, не жёсткая 1 — hit-stop (CORE-10) и пауза держат его не равным 1; проверено с `scale = 0.5`. Payload событий заморожен на уровне модуля — `emit` не аллоцирует. Вторая потеря во время восстановления латчит `_fatal`: `render()` навсегда no-op, цикла пересборки нет, не бросает. Пробел с общим прогревом — см. O-19. **Круг 1 не принят: восстановление рапортовало успех, а кадр был чёрным** — заглушечные тесты этого не поймали, нашёл только живой прогон. Причина найдена в исходниках three и стоит запоминания: (а) `initGLContext()` конструирует **новый** `WebGLBackground` при каждом восстановлении, а его конструктор безусловно ставит `clearColor = 0x000000`; `setClearColor()` — тонкий прокси на текущий экземпляр и через пересоздание не переживает; (б) наши слушатели висели **раньше** внутренних у `WebGLRenderer` (браузер зовёт их в порядке регистрации), поэтому пересборка шла по ещё мёртвым внутренностям three. Круг 2: слушатели регистрируются **после** конструктора рендерера, и при восстановлении заново применяются `outputColorSpace`, `toneMapping`, `setPixelRatio`, `setClearColor`, `autoClear`, `info.autoReset` |
| RNDR-2 | 2026-07-28 | `src/render/camera.js`, `tests/render/camera.test.js`, +геттер `cameraRig` в `src/render/index.js`, замена заглушки в `src/main.js`, +1 строка в `02-api-contracts.md` §1 | `npm run build`; весь набор **160/160**; **независимая сверка оркестратором**: FOV 35, near/far 1/60, решатель даёт ровно 22.0000 м и ровно 52.000°; **страж проверен поведением** — прямая запись бросает, `withCameraWrite` пропускает, чтения свободны, запись в кватернион мимо гейта тоже ловится; **живой браузер**: камера 35/1/60/16:9, страж в проде **выключен**, кадр не пустой, ошибок нет | Разделение владения зафиксировано брифом: `render` владеет **ригом** (построение + чистый решатель orbit-lock), `player` владеет **движением** (слежение, мёртвая зона, peek, затухание тряски) — это PLYR-1. Страж — `Proxy` с `set`-трапом на самих инстансах `camera.position`/`quaternion`, а не список методов: three мутирует приватные `_x/_y/_z/_w` кватерниона внутри `slerp`/`setFromRotationMatrix` мимо публичных аксессоров, и список методов такое пропустил бы. Ставится через `Object.defineProperty`, потому что three объявляет `position`/`quaternion` как `writable:false`. **Легальный путь для PLYR-1** — см. O-17 |
| RNDR-1 | 2026-07-28 | `src/render/index.js`, `src/render/composite.js`, `tests/render/rndr1.test.js`, +2 строки в `src/main.js`, +2 строки в `docs/spec/02-api-contracts.md` §1 | `npm run build`; **живая проверка оркестратором в headless Chromium** (`vite preview` + Playwright): WebGL2 доступен, `__READY__`, `outputColorSpace='srgb'`, `toneMapping=6` (AgX), центральный пиксель `[65,75,90,255]` — кадр **не пустой**, `drawCalls=1`, `programs=1` (ни одной компиляции шейдера в кадре), стадии B1–B13, ошибок в консоли нет | Реализовано: `renderer`, `screenSize`, `frameIndex`, `stats`, `render(ctx)`, `resize`, `withPrewarmTarget`, `prewarmMaterials`, `dispose`. Каскады/GTAO/bloom/SMAA/LUT — намеренно **не** трогал, это M8. **Ключевая находка:** правило `outputColorSpace`/`toneMapping` в three r180 **бинарно** — `currentRenderTarget === null ? renderer.outputColorSpace : LinearSRGBColorSpace` (`WebGLPrograms.js`), то есть важен факт «привязан таргет или нет», а не профиль таргета. Формулировка `ARCHITECTURE.md` («read off the currently bound target») это описывает неточно. Следствие: материал композита обязан компилироваться при **отвязанном** таргете, иначе прогреется линейный вариант и первый реальный кадр всё равно перекомпилирует. Использован `renderer.compile()`, а не `render()`, чтобы ничего не мелькало на канвасе |
| CORE-9 | 2026-07-28 (3 круга) | `src/main.js`, `index.html` (одна строка `<script>`), `tests/core/boot.test.js` | `npm run build`; весь набор **121/121**; **независимая сверка оркестратором**: тринадцать стадий B1→B13 в порядке и с владельцами ровно по таблице `11-flows.md` §1.2, пропуски `ui`/`fx`/`save` размечены с причинами, `ctx` несёт все 13 полей, камера 35/52/22, `deterministic` → `0x5eed1234` и два прогона совпадают вплоть до состояния корневого потока, недетерминированные расходятся, `Math.random()` во всём `src/` **ровно один** | Композиционный корень — `main.js` (решение O-4). Бут-лог: массив записей `{id, stage, owner, status, tMs, …}` на `engine.__boot` и в возврате `boot()`; `stage`/`owner` скопированы из таблицы дословно, поэтому лог сверяется с ней напрямую. Lockstep включается при `?lockstep=1` **или** при отсутствии `requestAnimationFrame` (то есть всегда под Node) — B13 прогоняет ровно `BOOT_FRAMES = 3` кадра через `engine.frame(FIXED_DT)`. **Круг 1 не принят:** `ctx.time` появлялся только после `registry.init()`, то есть был `undefined` в `init()` подсистем — а вся модель данных планирует против `ctx.time.step`, и `undefined + N` дал бы `NaN`, при котором `step >= nextTickStep` всегда ложь и тик молча никогда не срабатывает. Круг 2 строит `Engine` в B3 (как и требует таблица) и досыпает `engine.systems` после `init()`; `engine.js` при этом не тронут — `frame()` перечитывает `this.systems` каждый кадр. **Круг 3** (вскрылся после регистрации первой настоящей подсистемы в RNDR-1): (а) `resolveSize` читал `canvas.width` **раньше** `window`, а у `<canvas>` без атрибутов он равен 300×150 по стандарту HTML5 — ветка с размером окна была структурно недостижима, и вся игра рисовалась в 300×150. Теперь порядок `opts → window → clientWidth → canvas.width`, а вычисленный размер **штампуется** в `canvas.width/height` как выходной буфер, а не читается как источник. Подтверждено живым запуском: канвас стал 1280×720. (б) тест сверял число подсистем с литералом `3` — его роняла бы каждая следующая подсистема все девять этапов; теперь B5 несёт `ids`, и тест проверяет **порядок**, а не количество |
| CORE-8 | 2026-07-28 (2 круга) | `src/core/prewarm.js`, `tests/core/prewarm.test.js` | `npm run build`; `node --test tests/core/prewarm.test.js` — 18/18; весь набор 110/110. Круг 1 закрыт по D-3: проверено, что `setRenderTarget` остался только в комментариях, `scratchTarget` вычищен, `finally` охватывает вызов `withPrewarmTarget` | `export async function prewarm(engine, opts)`. Порядок — явный список из `11-flows.md` §1.4 (`render` первым, `fx` **всегда** исключён), не порядок инициализации: расхождение подтверждено, см. O-9. Снимок `rng.s0..s3`, всех восьми полей `time` и `_accum` восстанавливается в `finally` **безусловно**, в т.ч. когда хук бросил; бросивший хук не срывает остальных, `prewarm` не бросает никогда. Ноль импортов, ноль обращений к часам. **Инвариант «render target связан при компиляции» пока не обеспечен end-to-end** — см. O-10 |
| CORE-5 | 2026-07-28 (2 круга) | `src/core/rng.js`, `tests/core/rng.test.js` | `npm run build`; `node --test tests/core/rng.test.js` — 24/24; **независимая сверка оркестратором**: `u32()` совпал с отдельной реализацией xoshiro128\*\* на BigInt (другой арифметический путь) на трёх состояниях, включая `[1,2,3,4]` и `[0xffffffff,1,0x80000000,0x7fffffff]` | **Круг 1 не принят:** `weighted` умел только `{ключ: вес}`, что не покрывает лут-таблицы `04` §12.3 (`entries: [{kind, weight, sub}]` — форма теряла `sub`). Первопричина оказалась хуже симптома: одноаргументный вызов всегда шёл по объектной ветке, поэтому `weighted(массив)` делал `Object.keys(array)` и **молча возвращал строку-индекс вместо записи** — `resolveTC` получил бы `"0"` вместо `{kind:'nodrop'}`, не упав. Круг 2 разводит формы по `Array.isArray` первым проверкой. Пустой вход и суммарный вес ≤ 0 теперь бросают `RangeError` **до** розыгрыша (состояние потока не тронуто) — кривая лут-таблица падает громко, а не отдаёт последнюю запись. Круг 2 добавил третью форму. Итого три: `weighted(cands, weights)` → элемент; `weighted(entries)` над массивом с `.weight` → **саму запись** (план §1, `04` §12.3); `weighted({k:w})` → ключ (`06` §5.1). Все три тратят **ровно один розыгрыш** — проверено сверкой продвижения состояния, иначе поехал бы `12.D08`. `disc` равномерен по площади (`r = R·√u`), сигнатура `disc(radius, out?)`. Сидирование — SplitMix32 с защитой от всенулевого состояния. **Литеральные векторы в тесте — снимок собственной реализации, не внешний эталон** (в спеках эталона нет); алгоритм при этом сверен мной независимо |
| CORE-4 | 2026-07-28 | `src/core/events.js`, `tests/core/events.test.js` | `npm run build`; `node --expose-gc --test tests/core/events.test.js` — 12/12, **`12.A03` = 0 Б/вызов**; **независимая проба оркестратора**: 2 000 000 эмитов → 0.009 Б/эмит против 0.411 у наивной реализации со spread (разница 44×) | Generation guard = снимок `boundary = fns.length` до обхода: `on()` всегда добавляет в хвост, поэтому подписка во время эмита в этот эмит не попадает. `off()` **не сплайсит** (сдвинул бы живые индексы), а ставит tombstone; уплотнение — только когда все обходы этого бакета размотались (`Bucket.depth`). Вложенный `emit` получает собственные `boundary`/индекс. Бросивший обработчик логируется и не срывает остальных |
| CORE-6 | 2026-07-28 | `src/core/config.js`, `tests/core/config.test.js` | `npm run build`; `node --test tests/core/config.test.js` — 14/14; **сверка оркестратором всех четырёх пресетов по спекам построчно** | Все числа сошлись с источниками: `maxActors`/`maxSkinned`/`corpseBudget` — `08` §9.6 (по D-1), `groundItemBudget`/`particleBudget`/`decalBudget` — `01` §11.1, `shadowMapSize` — по D-2. **`q.audio` найден в `10-audio.md` §8.5** (таблица «Quality presets (`config.q.audio`)») и воспроизведён дословно, включая `music perc` и `grains ×0.5/0.75/1.0/1.5` — не выдуман. Дефолт `high`, `camPitch 52`, `camDist 22`, `stamina false`, пресеты заморожены |
| CORE-7 | 2026-07-28 | `src/core/input.js`, `tests/core/input.test.js` | `npm run build`; `node --test tests/core/input.test.js` — 17/17; **собственная проба оркестратора**: `beginFrame()+endFrame()` = 0 байт/кадр под `--expose-gc`, событие внутри окна кадра невидимо до следующего `beginFrame()` | Конструкция: два непересекающихся слоя. Сырой (`_keyEntries.live/downEdge/upEdge`, `_rawPointer*`) пишется только DOM-обработчиками и не достижим публично; снимок (`held/pressed/released`, `pointer`, `wheelDeltaY`) пишется **только** в `beginFrame()`. `endFrame()` гасит одноразовые рёбра и сырого слоя не трогает. `blur` ставит up-edge, а не правит снимок. Идентичность клавиши — сырой `KeyboardEvent.code` (см. O-6) |
| CORE-3 | 2026-07-28 | `src/core/registry.js`, `tests/core/registry.test.js` | `npm run build`; `node --test "tests/**/*.test.js"` — 25/25 | Топосорт — DFS post-order, стабилизирован **порядком `add()`** (не лексикографией): тот же порядок регистрации → тот же результат всегда. Цикл и неизвестная зависимость бросают из `resolve()` с перечислением участников. `get` бросает (раздельно «не зарегистрирован» / «не инициализирован»), `peek` возвращает `undefined`, `has` — true только после `init()`. `dispose()` — в обратном порядке. Паттерн для CORE-9: `const systems = await registry.init(ctx); new Engine({ systems, ctx, render })` |
| CORE-2 | 2026-07-28 | `src/core/engine.js`, `tests/core/engine.test.js` | `npm run build`; `node --test tests/core/engine.test.js` — 9/9, включая **`12.D07`** (600×1 / 300×2 / 100×6 → одинаковые `pos`, `ticks`, `step=600`) | Порядок кадра ровно по `11-flows.md` §2. Аккумулятор пополняется **масштабированным** `dt` → `time.scale = 0` — настоящая пауза. Спиральный сторож обнуляет аккумулятор на 6-м шаге; `100×6` в тесте бьёт ровно в кламп. Ноль импортов, Node-safe. Вход — `frame(rawDt)` (см. O-3). Изобретённый хук `world.serviceZoneRequest()` — см. O-1 |
| CORE-1 | 2026-07-27 | `package.json`, `vite.config.js`, `LICENSE-THIRD-PARTY`, `index.html` | `npm run build`; `npm run dev` + `curl 127.0.0.1` → 200, 324 B, верный `<title>`; `grep` копирайта | `index.html` разрешён сверх колонки Files: vite без него отдаёт 404, критерий «отдаёт страницу» иначе недостижим. HMR гасится через **`CLAUDO_NO_HMR=1`** — это имя должен использовать `tools/capture.mjs` (TEST-3). `base: './'`. Скрипт `test` = `node --test tests/`, реально заработает после TEST-1 |

### D-3 — scratch render target для прогрева связывает сам `render`

Противоречие (O-10): `ARCHITECTURE.md` §Pre-warm и `11-flows.md` §1.4 требуют, чтобы
target был связан на время компиляции и чтобы это делал `prewarm.js`;
`02-api-contracts.md` §1 «Forbidden for callers» запрещает `setRenderTarget()` вне
кадра `render`. Плюс `prewarm.js` не имеет права импортировать `three`.

Решение: **`RNDR-1` выставляет `render.withPrewarmTarget(fn)`** — сам строит
scratch-таргет 1×1, связывает, зовёт `fn`, восстанавливает прежний в `finally`.
`prewarm.js` выполняет весь проход внутри этого колбэка. Запрет из `02` §1
**остаётся абсолютным и не правится**: связывание живёт внутри кода самого
`render`, единственного владельца формата HDR-таргетов.

Обязательства:
- **RNDR-1** — реализовать `withPrewarmTarget(fn)` и **в том же тикете** добавить
  строку в `02-api-contracts.md` §1 (правило самой спеки: новый публичный метод =
  строка в таблице в том же коммите).
- **CORE-8** — вызывать его при наличии, деградировать без него, и держать снимок
  `rng`/`time`/`_accum` снаружи колбэка. Отправлено в доработку.

### D-6 — минимальный `actors.moveTo` вытянут из ACTR-2 (M1) в M0

**Ошибка последовательности в бэклоге.** `PLYR-1` (M0) обязан дать «капсулу,
которая идёт в точку клика». Но двигать актора он не вправе:
`02-api-contracts.md` §4 запрещает прямую запись `actor.x`/`actor.z` —
«Direct writes desync the body from the actor and **actors fall through
walls**», — а законный путь `actors.moveTo()` принадлежит тикету **ACTR-2,
стоящему в M1**. То есть M0 в расписанном виде недостижим: его собственная цель
требует метода из следующего этапа.

Решение оркестратора: вытянуть **только `moveTo`** в `src/actors/motion.js`
досрочно, силами ACTR-1 (владелец каталога, контекст жив). Это исправление
последовательности, а не расширение объёма — ни одного нового требования не
добавлено.

**Остаётся за ACTR-2 в M1:** `teleport`, `face`, скорость и **dev-страж на
прямую запись `actor.x/z`** (его критерий приёмки — «a direct write is caught by
a dev-build guard»). Файл `motion.js` создан досрочно и помечен комментарием.

### D-5 — хеш состояния включает **числовые** вторичные ресурсы, а не строку архетипа

`12-testing.md` §7 требует хешировать `id, x, z, life, mana, **secondary**, state,
actionSeq`. Но поля `secondary` на записи `Actor` нет:

- `01-data-model.md:415` — `secondary: 'rage'` лежит на записи **классового
  архетипа** (среди `trees`, `startingItems`, `startGold`) и это **строка**
  `'rage' | 'resonance' | null`;
- `01-data-model.md:286–288` — у `Actor` ресурсы **числовые**: `rage`,
  `resonance`, `stamina`.

Решение: хешируются **числовые** `rage`, `resonance`, `stamina`. Прочтение
«строка архетипа» отброшено по существу — она константа на весь прогон, в хеше
бесполезна, и хеш с ней **молча теряет** способность заметить расхождение в
начислении ярости или резонанса, то есть обесценивает `12.D04` и `12.D07`.
Три слота вместо одного — сознательно: хелпер не знает класс актора и не должен;
хешировать все три строго безопаснее, чем угадывать активный.

**Обязательство для ACTR-1:** запись `Actor` не должна обзаводиться полем
`secondary` — вторичный ресурс остаётся числовым (`rage`/`resonance`), а строка
`secondary` живёт только на архетипе класса.

### D-4 — TEST-2 и TEST-3: отложены и возвращены в тот же день

2026-07-28, первое решение: из трёх харнесс-тикетов M0 делать только TEST-1.
2026-07-28, **пересмотрено владельцем: TEST-2 и TEST-3 возвращены в очередь.**

Итог: **все три харнесс-тикета M0 делаются**, гейт M0 закрывается полностью, как
и задумано планом §12 и `12-testing.md` §14 («харнесс, написанный после
подсистемы, согласуется с кодом, а не со спекой, — и с багом согласуется так же
охотно»).

Порядок: TEST-1 → TEST-2 → TEST-3, **последовательно**, потому что все три
претендуют на секцию `scripts` в `package.json`, и параллельный запуск дал бы
конфликт в одном файле.

## Открытые интерфейсные вопросы (обязательны к учёту в будущих тикетах)

| # | Вопрос | Кого связывает | Решение на сегодня |
|---|---|---|---|
| O-40 | **`src/physics/separate.js` читает `performance.now()` внутри фиксированного шага — и `check-fixed.mjs` этого не видит.** `separate.js:177` и `:256` делают `const t0 = performance.now()` / `_stats.separationMs = performance.now() - t0` вокруг цикла разрешения. Это **не комментарий, а рабочий код**. При этом: `02` §4 объявляет `separate` как **`Fixed = Y`**, там же сказано, что «`actors` calls it exactly once per fixed step», а `ARCHITECTURE.md:263` формулирует правило прямо: «Nothing in `fixedUpdate` may read `performance.now()`, `Date.now()` or `dt`». **Найдено TEST-4** при попытке включить `src/physics/` в полный режим `check-imports` (O-29): переключение немедленно валит линтер на этих двух строках. Сабагент **не стал править чужой принятый файл** и остановился, как и должен был | **владелец проекта** (правка в принятом PHYS-4) + **TEST-16** (CI) — и это последний невыполненный пункт O-29 | **Почему это проскочило три приёмки.** `check-fixed.mjs` ищет чтения часов **в телах методов, названных `fixedUpdate`**. `separate()` — отдельный метод, который зовут **из** чужого `fixedUpdate`, поэтому под сканер не попадает. То есть модель линтера («метод называется `fixedUpdate`») уже, чем само правило («всё, достижимое из фиксированного шага»). Это дыра в инструменте, а не только один файл. **Насколько это опасно сегодня — умеренно:** значение уходит только в `stats.separationMs` и, как зафиксировано в строке приёмки PHYS-4, «измерение, никогда не возвращаемое в симуляцию», поэтому детерминизм пока не страдает. Опасность отложенная: правило существует ровно для того, чтобы никто позже не начал этим числом что-то регулировать. **Два варианта правки**, оба однострочные: объявить узкое исключение по образцу `src/core/` (там `window`/`document` легально читаются под `typeof`), либо вынести чтение часов за пределы проверяемого замыкания — измерять `separate()` снаружи, как `tools/profile.mjs` и так будет делать. **До решения `src/physics/` остаётся на `checkThree: true, checkGlobals: false`**, а `src/actors/`, `src/world/`, `src/nav/` уже переведены в полный режим и чисты |
| O-39 | **«Scope-тесты» — систематическая ловушка O-27, и они удалены как класс.** Каждый NAV-тикет писал тест вида «методы следующих тикетов ещё не существуют» (`typeof nav.smooth === 'undefined'` и т.п.). Такой тест **обязан** быть опровергнут следующим тикетом той же подсистемы, который просто делает свою работу. За два тикета это сработало трижды: NAV-2 уронил scope-тест NAV-1, NAV-3 уронил scope-тесты и NAV-1, и NAV-2. Первую попытку я решил сузить список — это была ошибка: NAV-4 (`buildFlowField`/`flowAt`/`flowDistance`/`flowVersion`) и NAV-5 (`snap`) опровергли бы его ещё дважды | **все будущие тикеты** | **Решение: scope-тесты удалены из `tests/nav/nav1.test.js` и `tests/nav/nav2.test.js` целиком**, на их месте комментарий со ссылкой на O-27. **Проверка объёма при этом не отменяется — она переносится туда, где ей место:** оркестратор сверяет реализованную публичную поверхность с таблицей `02-api-contracts.md` при приёмке, один раз, в момент ревью. Формулировка правила: **проверка объёма ценна как ревью-действие и вредна как постоянный ассерт.** Тест, который со временем гарантированно упадёт не по вине кода, приучает игнорировать настоящие падения. То же относится к любому ассерту вида «чего-то ещё нет» — счётчикам подсистем, тел, статик, пикселей (ровно список из O-27) |
| O-38 **(закрыт 2026-07-29)** | **ЗАКРЫТ в WRLD-3.** `_current`/`_staticFootprints` присваиваются теперь **до** вызова, и вызов идёт как `nav.rebuild(instance)`. Проверено оркестратором поведенчески, а не чтением: после входа в 60-метровый `last_bastion` сетка `nav` — 120×120, после 112-метрового `bonereach` — 224×224 (каждая совпадает со своей зоной); точка (50, 0) стала проходимой после входа в `bonereach`, что при старом дефекте было невозможно (границы прошлой зоны ±30 м). Ниже — исходное описание. **`world.enterZone` отдавал `nav` футпринты ПРЕДЫДУЩЕЙ зоны — отставание на один вызов.** В `src/world/index.js` порядок такой: строка 414 `emit('zone:enter')` → 423 `physics.rebuild()` → **427 `nav.rebuild()`** → 464 `this._current = instance` → 465 `this._staticFootprints = freezeFootprints(footprints)` → 467 `emit('zone:ready')`. То есть в момент вызова `nav.rebuild()` свойство `world.staticFootprints` ещё держит **замороженный массив прошлой зоны** (а на самом первом входе в процессе — начальный пустой), и `world.current` тоже ещё прошлый. При этом `nav` документирован как читающий именно `world.staticFootprints` (`07` §13 A1). Вдобавок вызов идёт **без аргумента** — `nav.rebuild()`, — тогда как контракт `02` §6 задаёт `rebuild(zone:ZoneInstance)`. **Найдено сабагентом NAV-1 на себе** (он не мог получить корректную геометрию через штатный путь) и **подтверждено оркестратором чтением строк** | **WRLD-3** — он владеет тем же `src/world/index.js`, и его критерий приёмки — ровно этот порядок эмиссии, проверяемый логом слушателя | **Мой промах на приёмке WRLD-1, и стоит зафиксировать почему:** моя проба WRLD-1 проверяла порядок событий и отсутствие утечки статик, но не могла проверить передачу в `nav` — **`nav` тогда не существовал**. Это тот же класс, что O-27, только с другой стороны: не «тест кодирует, что ничего нет», а «приёмка не видит интеграцию, которой ещё нет». Практический вывод для будущих приёмок: если тикет отдаёт данные подсистеме, которой ещё нет, отсутствие проверки надо **записывать как долг**, а не считать тикет полностью проверенным. **Правка мала:** перенести присваивания `_current`/`_staticFootprints` **выше** вызова `nav.rebuild()` и передать инстанс — `nav.rebuild(instance)`. Заморозка раньше `zone:ready` критерию WRLD-1 не противоречит (он требует «заморожен к `zone:ready`», а не «не раньше»). **Сегодня не ломает ничего наблюдаемого**, потому что геометрия WRLD-1 — временная заглушка, а NAV-2…NAV-5 тестируются на сетках, собранных руками, а не через `enterZone`. Но **PLYR-2 (200 прогонов по тестовой карте) пойдёт именно через штатный путь**, поэтому чинить обязательно до него — WRLD-3 стоит в очереди раньше. NAV-1 со своей стороны сделал вызов неломающимся: `rebuild()` без зоны деградирует до документированной заглушечной сетки 2×2 м и не бросает |
| O-37 | **`02-api-contracts.md` сам себе противоречит: смена зоны рехеширует статическую сетку ДВАЖДЫ.** §5 задаёт обязательный порядок эмиссии `zone:teardown → … → zone:enter → geometry built → **physics.rebuild()** → nav.rebuild() → zone:ready`, то есть `world` обязан позвать `rebuild()` **до** `zone:ready`. Но §4 в таблице событий `physics` пишет: «Listens \| `zone:ready` (**rebuilds the static grid**)» — то есть физика рехеширует сетку **ещё раз**, уже на самом событии. Обе секции по отдельности корректны, вместе дают два полных рехеша на каждый переход. **Замер оркестратора стек-трейсом** (счётчик вызовов показывал «2», но не говорил кто; трейс сказал): вызов №1 — `src/world/index.js:397`, законный, вызов №2 — `src/physics/index.js:332` ← `:312` ← `src/core/events.js:151`, то есть **собственный слушатель `zone:ready` в PHYS-1** | **владелец проекта** (правка затрагивает принятый PHYS-1 и/или строку спеки), потребители — **WRLD-3** (его критерий приёмки — именно этот порядок), WRLD-4 (M5) | **Убирать надо слушателя в `physics`, а не вызов в `world` — и вот почему.** Явный вызов `world` убрать нельзя: `nav.rebuild()` обязан растеризовать **уже перехешированную** сетку и обязан отработать **до** `zone:ready` (§5), а слушатель физики срабатывает уже во время `zone:ready` — то есть `nav` считал бы устаревшую сетку. Плюс критерий WRLD-3 требует, чтобы `physics.rebuild` был **виден в логе слушателя между `zone:enter` и `zone:ready`**, что достижимо только явным вызовом. Значит избыточен именно слушатель §4. **Цена сегодня невелика, поэтому не блокирует:** PHYS-1 хеширует 10 000 статик за 0.935 мс (медиана, худший 1.633 мс), то есть лишний рехеш стоит ~1 мс на переход при бюджете перехода ≤ 1100 мс (`07` §12 шаг 11). **Обязательство для WRLD-3:** его тест обязан проверять **порядок событий**, а не глобальный счётчик вызовов `rebuild` — иначе он упрётся в этот же вопрос. **Урок методологии, стоит запомнить:** я сначала отправил это WRLD-1 как его дефект по счётчику вызовов и потратил агенту круг; причина нашлась только когда я заменил счётчик на стек-трейс. Считать вызовы — значит знать «сколько», а не «кто»; на общем ресурсе вроде `physics` это разные вопросы |
| O-36 | **`tests/core/boot.test.js` запрещает подсистемам делать ровно то, что предписывает `ARCHITECTURE.md`.** Тест «prewarm … leaves `ctx.rng` untouched» жёстко утверждает, что весь зарегистрированный набор подсистем **не тратит ни одного розыгрыша** из корневого потока за время бута. Но `ARCHITECTURE.md` hard rule 4 и раздел «Determinism contract» предписывают обратное: «One `ctx.rng.fork()` per subsystem, **taken once in `init()`**». А `Rng.fork()` расходует **четыре** вызова `u32()`, чтобы засеять потомка. То есть первая же подсистема, которая выполнит прямое указание архитектуры, уронит этот тест. **Найдено сабагентом WRLD-1 на себе:** он взял `ctx.rng.fork()` в `init()` как велит правило — и получил красный `boot.test.js`, файл вне его владения | **любой тикет, дающий подсистеме первый настоящий RNG-поток** — ближайшие кандидаты: тикет с первым генератором `world` (`07` §1.8 требует потоки S0–S6), SKIL-*, ITEM-*, AI-* | **Это O-27 в пятый раз** — тест, написанный до подсистем, закодировал «пока никто не форкает». Счёт подсистем, тел, статик и пикселей уже ронял тикеты четырежды (CORE-9, TEST-2, PHYS-1, TEST-3); это тот же класс, только про поток RNG. **На WRLD-1 обошли законно, а не замазали:** его заглушечная геометрия — чистая функция от `descriptor.sizeX/sizeZ` и случайности не требует вообще, поэтому форк убран, а `_worldSeed` до вызова `setWorldSeed()` (загрузка сейва) держится на фиксированном задокументированном `0`. Прецедент тот же, что у PHYS-1/PHYS-2 («розыгрышей не делают — потока не берут»). **Обязательство:** тикет, который заведёт `world`'у настоящий генератор, обязан взять форк **и в том же тикете поправить `boot.test.js`** — этот тест должен проверять детерминизм (два бута с одним сидом дают одинаковое состояние потока), а не «никто не трогает корень», потому что второе противоречит архитектуре и будет ломаться на каждой следующей подсистеме |
| O-35 | **Запись `Footprint` определена в спеках дважды и по-разному, и расхождение деградирует молча.** `02-api-contracts.md` §4: `kind: 'box'\|'cylinder'\|'poly'`, `facing`, `y` + `height`, `blocksNav`, `blocksSight`, `points` — плоский массив. `07-world-gen.md` §1.7: `kind: 'box'\|'circle'\|'convex'`, `rotation` (кратно π/2), `baseY` + `topY`, `navBlock`, `points` — `Float32Array`, плюс отсутствующие в `02` поля `id`, `surface`, `destructible`. Совпадают только `x`, `z`, `halfW`, `halfL`, `radius`. **Опасность не в именах, а в тишине:** PHYS-1 разбирает вид через `KIND_CODE[fp.kind]` и при неизвестном ключе **молча откатывается на `KIND_BOX`** (`src/physics/index.js`, строка с `!== undefined ? … : KIND_BOX`), то есть цилиндрическая колонна, присланная как `'circle'`, станет коробкой без единого сигнала; ровно так же `fp.facing`/`fp.blocksNav` прочитаются как `undefined` и уедут в дефолты. Ни одного исключения, ни одного лога — обнаружится только как «монстры застревают об углы» через два этапа | **WRLD-1** (эмиттер), **WRLD-2**, **NAV-1** (оба потребители), WRLD-3 | **Разрешено оркестратором по тексту самой спеки, владельца не привлекал — это устаревший дубликат, а не живое противоречие.** `07` §13 в шапке: «**Status: applied.** A1–A6 are in `02-api-contracts.md` §§5, 9 and 4 — … and the `Footprint` record beside `Hit` and `MoveResult`. **Kept as rationale, not as a request.**» То есть §1.7 был свёрнут в `02` §4 и с тех пор существует как обоснование, а не как требование. Плюс `02` §4 сам себя объявляет анти-дрейфовой копией: «Recorded here so the two cannot drift (07-world-gen.md §1.7, §13 A6)». **Действующая форма — `02-api-contracts.md` §4, дословно.** Она же реализована и сверена поле-в-поле при приёмке PHYS-1 (двенадцать полей, дефолты совпадают). **Практические следствия, обязательные к выдаче в брифы WRLD-1/WRLD-2/NAV-1:** (1) виды называются `'box'`/`'cylinder'`/`'poly'`, **никогда** `'circle'`/`'convex'`; (2) поворот — `facing`, вертикаль — `y` + `height`, флаги — `blocksNav`/`blocksSight`; (3) `surface` **не поле записи** — это второй аргумент `physics.addStatic(footprint, surface, opts)`; (4) поля `id` и `destructible` из §1.7 физике неизвестны, но она читает только именованные поля, поэтому `world` вправе носить их на той же записи как собственную бухгалтерию — это безопасно и не нарушает контракт; (5) ограничение §1.7 «`rotation` кратен π/2» — свойство генератора, а не контракта: `02` §4 не ограничивает `facing`, и PHYS-1 корректно работает с произвольным углом (проверено пробой оркестратора на повёрнутых стенах при приёмке PHYS-5) |
| O-34 **(закрыт 2026-07-29)** | **ЗАКРЫТ в TEST-4** разделением прогона по путям: `npm test` = `test:unit && test:perf`, чувствительные файлы идут отдельной стадией с `--test-concurrency=1`. Пороги не тронуты, ретраев нет, ни один тест не стал недостижим. **Проверено оркестратором: пять зелёных прогонов `npm test` подряд, каждый 639 тестов.** Заодно закрывает O-28. Ниже — исходное описание. **Два теста в наборе флейкуют только под конкурентной нагрузкой полного раннера — и O-21 наконец воспроизведён.** При приёмке ACTR-2 оркестратор прогнал `npm test` четырнадцать раз. Полный набор дал **два разных падения**: (а) acceptance PHYS-4 «40 тел в круге 6 м: каждый вызов `separate()` ≤ 0.4 мс» — 2 падения из 14; (б) `assertAllocationFree(): the gate can fail` из `tests/helpers/alloc.test.js` — 1 падение из 14. **Изолированно оба идеально стабильны:** `tests/physics/phys4.test.js` — 14/14 восемь прогонов подряд; `tests/helpers/alloc.test.js` — 5/5 восемь прогонов подряд; `tests/actors/` — 80/80 восемь прогонов. **⚠️ Дефект ПРОГРЕССИРУЕТ с ростом набора — измерено на трёх точках (2026-07-29).** Частота падения acceptance-теста PHYS-4 в полном прогоне: при **379** тестах — ~1 прогон из 9; при **505** — 2 из 4; при **538** — **2 из 2**. Изолированно на тех же коммитах он даёт 14/14 (шесть и восемь прогонов подряд), то есть код не менялся — росла конкуренция за CPU внутри раннера. Экстраполяция прямая: `tests/nav/` (TEST-4) добавит ещё сотни тестов, и к концу M1 полный прогон будет красным **всегда**. **Практическое следствие для приёмки:** «`npm test` зелёный несколько прогонов подряд» перестал быть достижимым критерием вообще для любого тикета, поэтому оркестратор с этого момента принимает тикеты по схеме, которую O-34 и так предписывает для CI: полный набор **плюс** отдельный изолированный прогон трёх чувствительных групп (`tests/physics/phys4.test.js`, `tests/helpers/alloc.test.js`, `tests/tools/capture.test.js`), и падение в общем прогоне при зелёном изолированном не считается регрессией. Это обходной путь, а не починка — **починить обязан TEST-4 до гейта M1**, иначе пункт ② гейта не закрыть. **Третье проявление, найдено при приёмке WRLD-1 (2026-07-29):** под нагрузкой падают ещё и **браузерные** тесты `tests/tools/capture.test.js` — разом три (`non-blank PNG`, `two runs byte-identical`, `committed fixture matches`). Изолированно тот же файл даёт **21/21 три прогона подряд**, а запущенные вручную сразу после падения `capture.mjs` и `imagediff.mjs` дают `nonBlankPixels=921600/921600` и `diffPixels=0`. То есть это не регрессия рендера, а тот же дефицит CPU: файл поднимает `vite preview` + Playwright на уровне модуля (O-28), и под конкуренцией браузер не укладывается. **Значит затронуты все три класса: перф-бюджет, GC-проба и браузерный харнесс.** **Частота падения масштабируется с загрузкой CPU — измерено:** на спокойной машине падение PHYS-4 давало ~1 прогон из 9; когда оркестратор гонял тот же набор, пока в фоне работал ещё один сабагент, стало **2 прогона из 4**. Это прямое подтверждение, что меряется планировщик, а не код, и одновременно практическое правило: **не гонять `npm test` как приёмочную проверку, пока на машине работает сабагент**. **Это закрывает O-21:** предыдущий оркестратор сообщение TEST-3 о падениях `alloc.test.js` воспроизвести не смог (31 прогон, ноль падений) — теперь понятно почему: он гонял его в основном прицельно, а падает он только когда раннер Node параллельно исполняет другие файлы. Тогда же было верно замечено, что TEST-3 ловил это «когда параллельно работали другие сабагенты» — механизм тот же | **TEST-16** (CI), владельцы тестов PHYS-4 и TEST-1 | **Общая причина у обоих одна: ассерт на микросекундный бюджет или на GC-тайминг, исполняемый, пока раннер конкурентно грузит CPU другими файлами.** Сам алгоритм `separate()` тут ни при чём — независимый замер оркестратора при приёмке PHYS-4 дал max **0.0742 мс при бюджете 0.4 мс**, пятикратный запас; падение под нагрузкой доходило до ~1.02 мс, то есть меряется планировщик ОС, а не код. `12-testing.md` P2 флейки запрещает, и лечить это **ослаблением порогов нельзя** — порог 0.4 мс и есть смысл теста. Правильный путь тот же, что уже прописан в O-28: **разделять прогон по путям** и гонять перф/GC-чувствительные файлы отдельной стадией, без конкуренции (`--test-concurrency=1` либо отдельный вызов раннера), а не фильтром по имени. Это же снимает и O-24. Пока не сделано: `tests/` не принадлежит ни одному открытому тикету M1, кроме TEST-4 — **разумно выдать TEST-4 вместе с O-29**. **Следствие для гейта M1:** пункт ② («`npm test` зелёный, несколько прогонов подряд») сегодня **недостижим на полном наборе** не из-за продакшн-кода, а из-за харнесса; закрывать M1 с этим нельзя |
| O-33 **(закрыт 2026-07-29)** | **ЗАКРЫТ ремонтом — см. строку «O-33 fix» в таблице принятых.** Ловушки `has` и `getOwnPropertyDescriptor` добавлены, `hashState` по живому `actors.all` больше не слеп. Ниже — исходное описание, оставлено как история дефекта. **`actors.all` (ACTR-1) молча вёл себя как пустой список во всех индексных методах массива.** `makeAllViewHandler` в `src/actors/pool.js` реализует ловушки `get`/`set`/`deleteProperty`/`defineProperty`, но **не реализует `has` и `getOwnPropertyDescriptor`**, а цель `Proxy` — пустой массив. Поэтому `0 in all` === `false`, и спека `Array.prototype` (шаги с `HasProperty`) считает вид **сплошными дырами**. Замер оркестратора на трёх живых акторах: `all.length` = 3 ✔, `all[0].x` = 0 ✔, `for..of` = 3 ✔, `Array.from(all)` и `[...all]` = `[0,1,2]` ✔ — **но** `all.slice()` = `<3 empty items>`, `all.map(...)` = `<3 empty items>`, `all.filter(Boolean).length` = **0**, `all.forEach(...)` вызывается **0 раз**. Ни ошибки, ни предупреждения. **Как найдено:** оркестратор при приёмке ACTR-2 прогнал `hashState({ actors: actors.all, … })` до и после реального `moveTo` — хеш **не изменился**; на плоском `[actor]` тот же хеш меняется корректно (436943825 → 363542116). Причина: `hashState` канонизирует порядок через `.slice()` перед сортировкой, то есть получает массив дыр. **Это не дефект ACTR-2** — `pool.js` им не трогался (`git status` подтверждает), вид целиком принадлежит ACTR-1 | **владелец проекта** (решение по чужому принятому тикету); связывает **TEST-1**/`hashState`, весь гейт M2 (`03.E01`–`03.E14`, `12.D04`, `12.D07`), и любого будущего потребителя `all` | **Почему это опаснее, чем выглядит.** `02-api-contracts.md` §7 объявляет `all` как «property → `Actor[]` — the live dense list, **read-only**», то есть потребитель вправе ожидать обычный массив. Первый же, кто напишет `actors.all.forEach(...)` или `.filter(...)`, получит **тихий no-op**: цикл не выполнится ни разу, ошибки не будет, тест «ничего не упало» пройдёт. Отдельно: `12-testing.md` §7 строит на `hashState` весь детерминизм-гейт, а M2 существует ровно ради того, чтобы четырнадцать боевых примеров воспроизводились до последней цифры — с этим дефектом хеш по живому списку акторов **слеп к позиции**, и гейт будет зелёным, ничего не проверяя. Сегодня не проявилось только потому, что `hashState` пока никто не зовёт с живым `actors.all`. **Правка мала:** добавить в handler ловушки `has` и `getOwnPropertyDescriptor`, отвечающие теми же границами, что уже использует `get` (`Number.isInteger(idx) && idx >= 0 && idx < pool._liveCount`), плюс `ownKeys` при необходимости; дескриптор обязан быть `configurable: true`, иначе `Proxy` бросит инвариант на пустой цели. **Не сделано** потому, что `src/actors/pool.js` не входит в колонку Files ни одного открытого тикета M1 — это выход за бэклог (§12), решение владельца |
| O-32 **(закрыт 2026-07-29)** | **ЗАКРЫТ ремонтом — см. строку «O-32 fix» в таблице принятых.** Промахи при `radius > 0` снялись с 0.099% до нуля на 5074 пересечениях, радиус 0 сверен с grid-free эталоном. Ниже — исходное описание, оставлено как история дефекта. **`circleCast` (PHYS-3) собирал кандидатов по линии луча, а запрос — диск.** `cast.js`'s `_gatherStaticCandidatesAlongRay` обходит только ячейки, через которые проходит **центральная линия** сегмента. Сама же проверка — свип диска радиуса `radius`. Статик, захешированный в соседнюю ячейку, которую линия не задевает, в узкую фазу не попадает **никогда** и молча считается отсутствующим. Найдено оркестратором 2026-07-28 при независимой проверке PHYS-5 — у `sweep.js` был ровно тот же дефект, там он исправлен (см. строку PHYS-5), в `cast.js` **нет**. **Замер оркестратора, brute-force сверка с плотной выборкой, 20 000 испытаний на режим:** при `radius = 0` — **0 промахов на 2790 реальных пересечениях**; при `radius > 0` — **5 промахов на 5074 (0.099%)**. Границы дефекта проверены отдельно, чтобы не раздувать: `rayCast`/`lineOfSight` (радиус 0) — чисто; `overlapCircle`/`nearest` — чисто (тела сканируются линейно, сетка их не хеширует); **`moveBody` (PHYS-2) — чисто**, 4000 тел, заведённых в повёрнутые стены 0.2 м реальным шагом 0.07 м (4.2 м/с ÷ 60 Гц), ноль проникновений глубже 1 см, плюс прицельный тест на границе ячейки останавливается ровно на `1.7499` при грани `1.75`. **То есть обычная ходьба не затронута**, затронут только свип диска ненулевого радиуса | **владелец проекта** (решение по чужому принятому тикету), потребители — SKIL-* (M4, снаряды/рывок), PLYR-2 (M1) | **Почему это проскочило приёмку:** критерий PHYS-3 — «`lineOfSight` сходится с `rayCast` на 10 000 пар», а обе эти функции работают с радиусом 0, где дефекта нет. Критерий буквально не мог его увидеть — это не халатность сабагента, а дыра в самом критерии, и её стоит помнить при написании будущих «Done when». **Правка известна и мала:** перенести в `cast.js` то же расширение обхода до блока `(2·⌈radius/cellSize⌉+1)²`, которое уже сделано и проверено в `sweep.js` (при `radius=0` вырождается в прежнее поведение, то есть `rayCast`/`lineOfSight` не меняются вообще). **Не сделано** потому, что `cast.js` не входит в колонку Files ни одного открытого тикета M1, а трогать чужой принятый тикет без прямой команды владельца — выход за бэклог (§12). Цена по замеру в `sweep.js`: свип при `radius>0` дорожает ×3 (0.376 → 1.141 µs в сцене 800 статик), радиус 0 не дорожает |
| O-31 | **`.gitignore` делает пиксельный гейт невозможным.** В корневом `.gitignore` есть строка `shots/` (раздел «Capture / profiling output»). Git-шаблон без ведущего слэша матчит каталог с таким именем **на любой глубине**, то есть заодно и `tests/fixtures/shots/` — благословлённый эталон. При этом `tests/tools/capture.test.js` содержит ассерт «`tests/fixtures/shots/boot_clean.png` must be committed (12-testing.md §6)». Обнаружено оркестратором на **чистом клоне**: `npm test` → 379 тестов, **1 падение**, ровно этот ассерт; эталона в дереве нет вообще. То есть весь M0-гейт ⑤ (`imagediff` против эталона) в свежем чекауте не воспроизводим | **TEST-15** (`baseline.mjs`, M9), **TEST-16** (CI) | Правка на один символ: `shots/` → `/shots/` (только корневой каталог-свалка), либо явное исключение `!tests/fixtures/shots/`. **Сам файл `.gitignore` не трогал** — он вне владения M1-тикетов, а починка требует коммита, которого нет разрешения делать. Локально эталон восстановлен: `npm run build && node tools/capture.mjs --shot boot_clean --bless`, кадр **просмотрен глазами** (зелёная плоскость + светло-бежевая капсула, ровно как описано в строке PLYR-1), после чего `npm test` → 379/379. **Следствие для CI:** до починки `.gitignore` стадия «visual» на чистом клоне гарантированно красная |
| O-30 **(закрыт)** | **Пространственного индекса по телам нет.** Униформная сетка (PHYS-1) хеширует только **статику**. Поэтому в PHYS-3 `overlapCircle`/`overlapCone`/`overlapRect`/`nearest` и «телесная» половина `circleCast`/`rayCast` — линейный скан по всем телам, O(bodies), ограниченный сверху `q.maxActors` (24–56 по D-1). `ARCHITECTURE.md` описывает модель как «O(actors in a cell)». `separate()` при этом **пары**, то есть O(bodies²) | **PHYS-4** (закрыл), далее AI-5 (толпа) | **Закрыт замером в PHYS-4, а не постройкой индекса.** 56 тел (потолок `q.maxActors`) = максимум 1540 пар; независимый замер оркестратора на 1000 вызовах `separate(4)` дал p50 = 0.0071 мс, p99 = 0.0342 мс, **max = 0.0742 мс при бюджете 0.4 мс**. Строить телесную сетку сейчас — платить сложностью за 5–50× уже имеющегося запаса. **Обязательство:** любой тикет, поднимающий `q.maxActors` заметно выше 56 (кандидат — AI-5), обязан **перемерить**, а не унаследовать это решение; помечено и в шапке `separate.js` |
| O-1 | Фаза кадра 4b (C-3) обязана обслужить залатченный `world.requestZone()`, но **в `02-api-contracts.md` нет метода для самого обслуживания** — есть только `requestZone` (латч) и `enterZone` (`Fixed = N`). CORE-2 вызывает `world.serviceZoneRequest()` — это имя **изобретено**, в спеке его нет | WRLD-4 (`07 §12.4`) | Вызов защищён `typeof`-гардом и до появления `world` — no-op, ничего не ломает. WRLD-4 обязан либо реализовать `serviceZoneRequest()`, либо согласовать другое имя и **в том же тикете** поправить строку в `02-api-contracts.md` §5 и вызов в `engine.js` |
| O-2 | `node --test tests/` (директорией) на **Node 26 падает** `MODULE_NOT_FOUND` — рантайм трактует путь как модуль. Рабочая форма: `node --test "tests/**/*.test.js"` | TEST-1 | Скрипт `test` в `package.json` сейчас в нерабочей форме. TEST-1 обязан привести его к глобу; до этого проверяю тесты явной формой |
| O-23 | **Проба `12-testing.md` §4.4 непригодна для полиморфных горячих путей.** Её прогрев — **один** вызов `fn()` перед замером, а `12.A01` требует «< 1 байта/вызов усреднённо по 10 000». Измерено оркестратором на `moveBody` со **двумя** видами статик (box + cylinder): при N = 10k / 50k / 200k / 1M / 4M выходит 77.1 / 27.2 / 8.0 / 1.5 / 0.33 байт/вызов, а **суммарные** байты держатся ~1.3–1.6 МБ при росте N в 400 раз. То есть утечки нет — есть фиксированная стоимость стабилизации inline-кэшей, но по букве `12.A01` на 10 000 вызовов метод **не проходит** | **TEST-6** (M2, аллокационные пробы) | Чинить **удлинением прогрева**, а не ослаблением порога: настоящая утечка держит константу байт/вызов при любом N (в черновике PHYS-2 это было ~1052 Б/вызов), а разовая стоимость падает как 1/N. Признак для различения — смотреть **суммарные** байты, а не среднее. `assertAllocationFree` из TEST-1 частично это уже обходит выборкой раундов |
| O-22 | **`src/physics/` не входит в корни `check-imports`.** `12-testing.md` §2.1 перечисляет `src/combat/`, `src/items/`, `src/skills/`, `src/nav/`, `src/world/data/` и каждый `data/` — физики там нет. Но `02-api-contracts.md` §4 прямо объявляет, что physics «runs headless in Node», и PHYS-1 это выдержал добровольно | **TEST-2** (задача выдана, запускать после PHYS-2) | Ничто эту границу не охраняет: PHYS-2…PHYS-5 могут импортировать `three`, и никто не заметит, пока в M5 не сломается `mapgen`. Добавить `src/physics/` корнем на проверку `three` — по образцу того, как уже добавлен `src/core/` |
| O-29 | **`src/physics/` и `src/actors/` добавлены в `check-imports` только на `three`, а не в полном N-surface режиме.** Сделано по образцу `src/core/`, где послабление оправдано: `config.js` и `input.js` легально читают `window`/`document` под `typeof x !== 'undefined'`. У физики и акторов такой причины **нет** — `02-api-contracts.md` §4 прямо называет физику «runs headless in Node», что ближе к `combat`/`items`, чем к ядру | **первый тикет M1, трогающий линтеры** | Рекомендация TEST-2, и я с ней согласен: включить им `checkGlobals` (полный режим — `three` + `document` + `window` + `performance.now()`). Проверено: оба каталога сегодня чисты, правка ничего не сломает, это одна строка на корень. Не сделано только потому, что M0 закрыт и действует стоп-правило владельца |
| O-28 | **`--test-name-pattern` не спасает от исполнения файлов.** Фильтр отбирает, какие тесты *запускать*, но все файлы всё равно загружаются и исполняют код уровня файла. `tests/tools/capture.test.js` при этом поднимает `vite preview` и Playwright — прогон `12.D07` через глоб с фильтром повис у оркестратора на 2 минуты, прицельный запуск по `tests/core/engine.test.js` отработал мгновенно | **TEST-16** (CI) | `12-testing.md` §10 обещает четыре стадии CI **под девять минут**, из них «unit» — 60 с. Если эта стадия каждый раз тащит браузерные тесты, бюджет не сойдётся. Разделять надо **по путям** (`tests/core/`, `tests/physics/`, … против `tests/tools/`), а не фильтром по имени |
| O-26 | **`tools/capture.mjs` не пересобирает `dist/`.** Запуск без предварительного `npm run build` молча снимает кадр со **старого** кода. На это независимо напоролись и PLYR-1, и оркестратор | **TEST-15** (`baseline.mjs`, M9), **TEST-16** (CI) | Опасность не в неудобстве: так можно «подтвердить» несуществующую починку или **заблагословить эталон со старой сборки** — и получить зелёный пиксельный гейт, который ничего не проверяет. Инструмент обязан либо пересобирать сам, либо падать с внятным сообщением, если `dist/` старше `src/` |
| O-27 | **Класс дефекта, проявившийся за этап четырежды: тест, написанный до подсистемы, кодирует «пока ничего нет».** `CORE-9` сверял число подсистем с литералом `3` (уронил RNDR-1); `TEST-2` предполагал, что в `src/` нет ни одного `fixedUpdate` (уронил PLYR-1); `PHYS-1` ждал нулевую `stats` (уронил PLYR-1); `TEST-3` зашил центральный пиксель `[65,75,90]` пустой сцены (уронил PLYR-1) | **все будущие тикеты** | Правило: **не сверяй с «пусто» — сверяй с тем, ради чего тест написан.** Число подсистем, тел, статик и цвет пикселя будут меняться все девять этапов. За пиксельную точность отвечает `imagediff` против эталона, а не ассерт в тесте инструмента. Обязательно вынести в бриф каждого тикета M1 |
| O-24 | **Настоящая гонка в тестах, а не шум.** `tests/tools/check-imports.test.js` создаёт и удаляет **реальный** `src/combat/` и `src/__ci_test_shared__/`, а `tools/check-fixed.mjs` сканирует **весь** `src/`. Раннер Node гоняет файлы тестов конкурентно → второй читает дерево, пока первый его меняет. Оркестратор воспроизвёл: один прогон из ~17 дал **7 падений разом** (валится файл целиком, а не отдельный ассерт). PHYS-2 уже латал `ENOENT` от `readdirSync` по этой же причине | **TEST-2** | Чинить изоляцией, а не ретраями (`12-testing.md` P2). Правильный путь — дать линтерам `--root <dir>`, чтобы тесты работали на временном дереве **вне** `src/`, и никогда не трогали настоящий `src/`. Выдано TEST-2 вместе с O-22 |
| O-25 | **Ограничение `moveBody`: слишком большой шаг туннелирует.** Схема PHYS-2 — «переместиться в цель, затем вытолкнуться из того, что перекрывает». При дельте, уносящей тело **за** препятствие, перекрывать нечего — тело проходит насквозь, `blocked: false`. Поймал ACTR-1 на `moveTo(a, 10, 0)` сквозь стену шириной 2 м | **SKIL-8** (рывок/блинк, M4), ACTR-2 | На обычной ходьбе безвредно: 4.2 м/с ÷ 60 Гц = 0.07 м за шаг. Но **рывок и блинк перемещают на метры за один шаг** — они обязаны либо разбивать перемещение на подшаги, либо идти через `physics.sweepProjectile` (PHYS-5), а не через `moveTo`. Снаряды уже покрыты отдельным `sweepProjectile` — ровно по этой причине |
| O-21 | TEST-3 сообщил, что `tests/helpers/alloc.test.js` падает примерно 1 раз из 4. **Оркестратор воспроизвести не смог: 31 прогон** — 20 прицельных, 5 полных наборов начисто, 6 полных под нагрузкой (три параллельных сборки) — ноль падений | наблюдение | Сообщение достоверное: проба GC-таймингозависима, а TEST-3 гонял её, когда параллельно работали другие сабагенты. `12-testing.md` P2 флейки запрещает, поэтому: если всплывёт в CI — чинить **увеличением `maxRounds`**, а не ослаблением порога. Ослабить порог значит сделать невидимой настоящую регрессию в ~23 Б/вызов, а это «гейт, который не может упасть» |
| O-20 | **Ловушка Node-раннера:** `--test-name-pattern` **после** глоба **молча не фильтрует** и прогоняет весь набор — ни ошибки, ни предупреждения. Рабочая форма — флаг **перед** глобом: `node --expose-gc --test --test-name-pattern='E13' "tests/**/*.test.js"`. Отдельно: **неэкранированный** глоб раскрывает шелл, и набор файлов получается другой (на том же дереве 199/5 пропусков вместо 197/0) | TEST-3, TEST-16 (CI), любой тикет, строящий команду запуска | Кавычки обязательны, флаг фильтра — всегда первым. Найдено TEST-1 при самопроверке |
| O-19 | `11-flows.md` §13.4 требует при восстановлении контекста заново прогнать **общий** `prewarm(engine)`. `render` этого сделать не может: `prewarm()` принимает экземпляр `Engine`, а подсистеме доступен только `ctx`, ссылки на движок в нём нет, а импорт `src/core/` из `src/render/` запрещён правилом владения | **CORE-9** (выдано) | `render` пересобирает свои ресурсы и свой композит сам — это работает. Общий диспетчер обязан перезапустить `main.js`: одна подписка на `render:context-restored` → `prewarm(engine)`. Без этого первый кадр после восстановления заплатит те самые 640–900 мс компиляций |
| O-17 | Как `player` легально пишет камеру (hard rule 2 запрещает ему импортировать `src/render/camera.js`) | **PLYR-1** | Через `ctx.get('render').cameraRig` — закэшировать в своём `init()`. Каждый кадр, **в `update()`**, не в `fixedUpdate`: `rig.solveOrbitLock(fx, fy, fz, ctx.config.camPitch, 0, ctx.config.camDist, outPos, outLookAt)`, поверх этого свои мёртвая зона / сглаживание / peek / shake, и запись **только** внутри `rig.withCameraWrite(ctx.camera, () => { … })`. Любая запись мимо гейта бросает в dev-сборке |
| O-18 | `far = 60` у мировой камеры: если `sky` нарисует **буквальный купол-меш** радиусом больше ~55 м, он будет обрезан плоскостью отсечения | **SKY-1** (M8) | Числа near/far выведены RNDR-2 из геометрии рига: при 22 м / 52° / FOV 35 ближняя видимая земля ≈18.5 м, дальняя кромка ≈30.6 м, углы кадра ≈35 м; `far=60` покрывает это плюс 40 м теней из плана §5. `far/near` = 60 против 5000 у прежней заглушки — на два порядка лучше точность буфера глубины. SKY-1 обязан либо держать радиус купола <55 м, либо рисовать небо depth-independent приёмом |
| O-13 | Движок каждый кадр зовёт покадровую композицию `render`, и `engine.js` с `11-flows.md` §2 (фаза 5) называют её `render(ctx)` — но **в `02-api-contracts.md` §1 такого метода в таблице нет** (там `renderer`, `registerPass`, `addLight`, `requestEnvMap`, `screenSize`, `depthTexture`) | **RNDR-1** | `main.js` зовёт утиным типированием: `const r = ctx.peek('render'); if (typeof r?.render === 'function') r.render(ctx);` — сегодня это ветка no-op. RNDR-1 обязан либо реализовать метод с именем `render(ctx)`, либо назвать настоящее имя и **в том же тикете** добавить строку в `02-api-contracts.md` §1 |
| O-14 | `?seed=` документирован в `11-flows.md` §1 B2 как читаемый параметр, но §14.2 даёт формулу `worldSeed` **без** ветки для него | — | CORE-9 добавил среднюю ветку: `deterministic ? 0x5eed1234 : (config.seed ?? (Math.random()*2**32)>>>0)`. Без явного `?seed=` поведение побайтово совпадает с формулой §14.2. Принято |
| O-15 | Плоскости отсечения мировой камеры и кадрирование UI-камеры **не заданы нигде** в `docs/spec/` | RNDR-2, UI-10 | CORE-9 поставил заглушки: мир `0.1/500`, UI `45°`, `0.1/50`, камера UI в `(0,1,3)` смотрит в `(0,1,0)`. Помечено комментариями в коде. Заменить в RNDR-2 |
| O-16 | `?prewarm=0` документирован, но принятый `prewarm.js` флаг `config.prewarm` **нигде не читает** | — | CORE-9 закрыл флагом сам вызов B8 в `main.js` — единственное место, где флаг может что-то значить без правки чужого файла. Принято |
| O-12 | `main.js` — единственная точка, где подсистемы регистрируются (`registry.add`), но он **lead-owned** и в колонке Files есть только у CORE-9. При этом каждая новая подсистема (RNDR-1, PHYS-1, ACTR-1, PLYR-1 и далее все 16) обязана в него попасть, иначе не загрузится | все тикеты подсистем | **Решение оркестратора:** каждому тикету, создающему новую подсистему, **явно разрешается** добавить в `src/main.js` ровно две строки — `import` своего класса и `registry.add(...)` в блоке регистрации. Больше в `main.js` — ничего. Разрешение выдаётся в брифе тикета поимённо, а не по умолчанию. Альтернатива (динамический манифест) не годится: vite не соберёт `import` несуществующего модуля |
| O-9 | Порядок прогрева в `11-flows.md` §1.4 (`render → materials → sky → world → actors → **items** → **skills**`) **не совпадает** с порядком инициализации в `02-api-contracts.md` (`skills` раньше `items`) | ITEM-15, SKIL-*, RNDR-8 | Реализован порядок §1.4 как явный список приоритета; подсистемы, реализующие хук, но в списке не названные, идут после — в порядке инициализации. На M0 безвредно (хук не реализует никто) |
| O-10 | **Противоречие спек.** `ARCHITECTURE.md` §Pre-warm и `11-flows.md` §1.4 требуют, чтобы `prewarm.js` связал scratch render target 1×1 перед компиляцией. `02-api-contracts.md` §1 в разделе «Forbidden for callers» запрещает: «Never call `renderer.render()`, **`setRenderTarget()`**, `clear()`, `setViewport()` … outside `render`'s own frame». Плюс `prewarm.js` не имеет права импортировать `three`, значит и построить `WebGLRenderTarget` не может | **RNDR-1, CORE-9** | Временно: `prewarm(engine, { scratchTarget })` — если target передан, связывается через документированное свойство `render.renderer` и прежний восстанавливается; если нет (сегодня — всегда) — корректный no-op. **Инвариант 1 не обеспечен никем.** Требует решения владельца проекта — см. ниже |
| O-11 | `11-flows.md` §1.4 показывает `render.prewarmMaterials({ post: true, shadow: false })` с объектом опций, а `02-api-contracts.md` §1 документирует сигнатуру как `(ctx) => Promise<void>`, как у всех остальных | RNDR-1 | Все семь хуков зовутся единообразно `prewarmMaterials(ctx)` — по документированному контракту, а не по картинке в таблице. Если `render` действительно нужны опции, это правка строки в `02` §1 в тикете RNDR-1 |
| O-7 | `createConfig` отдаёт только флаги `deterministic`, `seed`, `lockstep`, `prewarm` — **вывод `worldSeed` в нём не живёт** | CORE-9, TEST-2 | **Закрыт спекой.** `11-flows.md` §14.2 задаёт дословно: `worldSeed` при `config.deterministic === false` — это `(Math.random() * 2 ** 32) >>> 0`, берётся **один раз**, **в `main.js`**, сразу пишется в сейв и больше не берётся никогда; при `?capture=1` — `0x5eed1234`. И там же: «**This is the only `Math.random()` call that exists**». То есть у hard rule 4 ровно одно задокументированное исключение, и оно в `main.js`. **TEST-2 (`check-imports`/`check-fixed`) обязан знать про эту единственную легальную площадку** — иначе либо завалит сборку, либо, что хуже, заставит кого-то её «починить» |
| O-8 | Неизвестное имя пресета качества в `?q` **бросает**, а не откатывается на `high` (решение CORE-6) | TEST-3, TEST-14 | Сознательно: харнесс с опечаткой в `?q` обязан падать заметно, иначе молча померит другой бюджет. Инструменты, дёргающие страницу с `?q`, должны передавать валидное имя |
| O-5 | Имена аксессоров снимка ввода — `keyHeld/keyPressed/keyReleased(code)`, `buttonHeld/buttonPressed/buttonReleased(button)`, свойства `pointer:{x,y}` и `wheelDeltaY` — **выбраны сабагентом**, в `02-api-contracts.md` их нет (там `input` вообще не описан как публичная поверхность) | PLYR-1 | Оставлено как есть: `input` — часть ядра, а не подсистема из таблицы владения, и его поверхность спека не фиксирует. PLYR-1 потребляет эти имена; если ему удобнее другие — меняем **в тикете PLYR-1**, одним заходом, вместе с `input.js` |
| O-6 | Идентичность клавиши — сырой `KeyboardEvent.code`: `ShiftLeft` и `ShiftRight` — **две разные клавиши**, обобщённого `Shift` нет | PLYR-1, UI-3 | Сознательно: ядро не навешивает смысл на клавиши (это работа `player`/`ui`). Но `11-flows.md` §3.2 использует «Shift held» как одно условие — потребитель обязан проверять обе клавиши, иначе Shift-модификатор молча не сработает на одной из сторон клавиатуры |
| O-4 | `11-flows.md` §1 (B3–B5) описывает **`Engine` как фасад**: `new Engine({canvas, config})` строит `scene`/`camera`/`uiScene`/`uiCamera`/`EventBus`/`Input`/root `Rng`, затем `engine.add() × 16` и `await engine.init()`. Реальный `src/core/engine.js` (CORE-2) — **чистый цикл**: ни `add`, ни `init`, ни сцены; подсистемы приходят снаружи готовым массивом, регистрация и топосорт живут в `Registry` (CORE-3) | CORE-9 | **Решение оркестратора: композиционный корень — `main.js`, а `Engine` остаётся чистым циклом.** Причина: если отдать `Engine` сцену и камеру, конструирование камеры окажется в `engine.js` и столкнётся с владением `src/render/camera.js` (RNDR-2). Наблюдаемое поведение стадий B1–B13 (порядок, бут-лог, бюджеты) воспроизводится одинаково в обоих вариантах, поэтому критерий CORE-9 «boot log matches §1's stage table» достижим. Расхождение — только в том, на каком объекте живут `add`/`init` |
| O-3 | Явный вход в кадр — `engine.frame(rawDt)`, возвращает число исполненных фиксированных шагов; `start()`/`stop()` — тонкая rAF-обёртка и единственное место с браузерными глобалями | TEST-3, CORE-9 | Lockstep-помпа `src/dev/shots.js` и `tools/capture.mjs` обязаны гнать кадры через `frame(rawDt)`, а не через rAF |

## Решения владельца проекта по расхождениям в спеках

Приняты 2026-07-28. **Обязательны к соблюдению во всех последующих тикетах** —
это разрешение противоречий, а не предпочтение оркестратора.

### D-1 — `q.maxActors` = 24 / 32 / 44 / 56, ёмкость пула `Actor` — отдельная величина

Противоречие: `01-data-model.md` §11.1 даёт пулу `Actor` числа 60/100/160/220 и
аннотирует их `(q.maxActors)`; `08-characters-visual.md` §9.6 даёт `q.maxActors`
= 24/32/44/56.

Решение: **побеждает `08` §9.6.** `q.maxActors` — симуляционный потолок,
24/32/44/56. Аннотация `(q.maxActors)` в `01` §11.1 признана опечаткой:
60/100/160/220 — это ёмкость пула записей `Actor`, отдельная величина, владелец
— `actors` (ACTR-1). Пул строго больше потолка, поэтому обещание `01` §11.1
«never overflows» остаётся верным, а запас уходит под квестовых актёров и трупы.

Согласовано с: `08` §1.4 (escape hatch 44→30), `06` §12 (`corpseBudget`
6/10/16/20), планом §0 («на экране 10–25 мобов»). Переписывать эти документы не
нужно — они уже согласованы между собой.

### D-2 — `shadowMapSize` = 1024 / 2048 / 2048 / 4096, назначено, а не выведено

Ни одна из тринадцати спек значения не задаёт. Числа **назначены владельцем
проекта**; `medium` и `high` намеренно равны — разница между ними в числе
каскадов, а не в разрешении карты. Владелец на пересмотр — **RNDR-4 (M8)** по
результатам замеров; менять только там и с записью в этот журнал.

## Гейты этапов

| Этап | Гейт | Результат |
|---|---|---|
| M0 | `npm run build`; `capture.mjs --shot boot_clean` non-blank; `check-imports` green; `12.D07` on an empty world | **зелёный** (прогнан 2026-07-28, таблица в шапке; перепроверен на чистом клоне после восстановления эталона — см. O-31) |
| M1 | `12.P08` (`nav.rebuild ≤ 3 мс`), `12.P09` (A\* ≤ 1 мс); `tests/nav/` зелёные; капсула проходит тестовую карту 200 прогонов без заклинивания | **ЗЕЛЁНЫЙ** (прогнан оркестратором 2026-07-29, все восемь пунктов — таблица ниже) |
